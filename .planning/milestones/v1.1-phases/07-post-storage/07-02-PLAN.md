---
phase: 07-post-storage
plan: 02
type: execute
wave: 2
depends_on: [07-01-PLAN.md]
files_modified:
  - src/content/index.ts
autonomous: true
requirements: [STORE-01]

must_haves:
  truths:
    - "content/index.ts imports persistStoredPost from '../shared/postStore'"
    - "persistStoredPost is called (fire-and-forget) inside the if (hide) block when a post is hidden"
    - "postText is passed as the text argument (already available in the callback closure)"
    - "authorId || urn is used as the authorId argument (matches persistFlaggedAccount call site)"
    - "npx tsc --noEmit exits 0"
  artifacts:
    - path: "src/content/index.ts"
      provides: "Wired hide path — calls persistStoredPost on every hidden post"
      contains: "persistStoredPost"
  key_links:
    - from: "src/content/index.ts"
      to: "src/shared/postStore.ts"
      via: "import { persistStoredPost } from '../shared/postStore'"
      pattern: "from ['\\\"].*postStore['\\\"]"
---

<objective>
Wire `persistStoredPost` into the content script's hide path.

One import, one call. The `postText` variable is already in scope in the `startObserving` callback. The call is fire-and-forget (`.catch(() => {})`), consistent with `persistFlaggedAccount` and `writeDailyStats`.

After this plan, every auto-hidden post is saved to `storedPosts` in `chrome.storage.local` for Phase 8 to display.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-post-storage/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Import and call persistStoredPost in content/index.ts hide path</name>
  <files>src/content/index.ts</files>
  <read_first>
    - src/content/index.ts (find the import block at the top and the `if (hide) { ... }` block in startObserving)
    - src/shared/postStore.ts (after Plan 01 — confirm the exported function name and parameter shape)
    - .planning/phases/07-post-storage/07-CONTEXT.md §D-08, §D-09
  </read_first>
  <action>
    Modify `src/content/index.ts`:

    **1. Add import** (alongside the other shared/ imports):
    ```typescript
    import { persistStoredPost } from '../shared/postStore';
    ```

    **2. In the `if (hide) { ... }` block**, add the fire-and-forget call after `writeDailyStats().catch(() => {})`:
    ```typescript
    persistStoredPost({
      urn,
      authorId: authorId || urn,
      authorName,
      score: mergedScore,
      text: postText,
    }).catch(() => {});
    ```

    The variables `urn`, `authorId`, `authorName`, `mergedScore`, and `postText` are all already in scope in the `startObserving` callback closure. No new extraction needed.

    Do NOT change any other logic.
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('src/content/index.ts','utf8'); if(!s.includes('persistStoredPost')){console.error('import missing');process.exit(1);} if(!s.includes('text: postText')){console.error('postText not passed');process.exit(1);} console.log('content/index.ts ok');" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `persistStoredPost` is imported from `'../shared/postStore'`
    - The call is inside `if (hide) { ... }` with `urn`, `authorId: authorId || urn`, `authorName`, `score: mergedScore`, `text: postText`
    - Called as fire-and-forget with `.catch(() => {})`
    - `npx tsc --noEmit` exits 0 across the whole project
  </acceptance_criteria>
  <done>Every auto-hidden post is now persisted to storedPosts; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-04 | Information Disclosure | postText passed to persistStoredPost | accept (user opt-in) | User explicitly opted in; text is local-only; truncation happens in postStore.ts |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` succeeds
- Manual: load extension, scroll LinkedIn, let a post be hidden → open DevTools → Application → Storage → `storedPosts` array appears with `urn`, `authorId`, `score`, `text`, `hiddenAt`
- Verify text is truncated at 1000 chars for long posts
- Browse the same post again → dedup fires, array length does not increase
</verification>

<success_criteria>
- STORE-01: hidden posts are saved to `storedPosts` with all required fields ✓
- STORE-02: 200-entry cap enforced in postStore.ts ✓
- STORE-03: text truncated at 1000 chars ✓
- Phase 8 can read `storedPosts` and filter by `authorId` to show per-account post previews
</success_criteria>

<output>
Create `.planning/phases/07-post-storage/07-02-SUMMARY.md` when done.
</output>
