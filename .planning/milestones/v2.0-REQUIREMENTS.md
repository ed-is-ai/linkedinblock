# Requirements Archive — v2.0 Chrome Web Store Release

**Archived:** 2026-05-31  
All v2.0 requirements shipped and validated.

---

## Manifest & Icons

- [x] **CWS-01**: `manifest.json` has all CWS-required fields: `icons` (16/48/128), `action.default_icon`, `homepage_url`, version `1.2.0`
- [x] **CWS-02**: PNG icon files at `src/public/icons/icon-16.png`, `icon-48.png`, `icon-128.png`; consistent branded design (LinkedIn blue shield); present in `dist/icons/` after build

## Store Content

- [x] **CWS-03**: `PRIVACY.md` in repo root covers all data categories, local-only storage, opt-in LLM, deletion instructions — accessible as raw GitHub URL
- [x] **CWS-04**: `store/LISTING.md` contains name, 132-char short description, 340-word detailed description, Productivity category, permissions justification, 5-screenshot checklist

## Packaging & Submission

- [x] **CWS-05**: `npm run package` builds and produces `dist/linkedin-blocker-v1.2.0.zip` (27.6 KB); ZIP contains `manifest.json` at root level
- [x] **CWS-06**: `store/SUBMISSION_GUIDE.md` covers registration ($5 fee), ZIP upload, store listing fields, privacy practices tab, review timeline, future update process

---

## Outcomes

| Req | Outcome |
|-----|---------|
| CWS-01 | Validated — manifest passes `vite build` with all fields; `tsc --noEmit` clean |
| CWS-02 | Validated — 3 PNG files in dist/icons/ confirmed after build |
| CWS-03 | Validated — PRIVACY.md committed to repo root; raw URL accessible |
| CWS-04 | Validated — short description exactly 132 chars; all sections present |
| CWS-05 | Validated — 27.6 KB ZIP produced; `npm run package` exits 0 |
| CWS-06 | Validated — all 6 steps present; URLs and values ready to paste |

## Post-archive note

After renaming the repo from `linkedinblock` to `linkedinaivoiceblock`, update the URLs in `PRIVACY.md` and `store/LISTING.md` to reflect the new repo name.
