---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - eslint.config.js
  - .prettierrc
  - .gitignore
  - src/manifest.json
  - src/content/index.ts
  - src/background/index.ts
  - src/popup/index.html
  - src/popup/index.tsx
autonomous: false
requirements:
  - INFRA-01
user_setup: []
tags:
  - chrome-mv3
  - vite
  - typescript
  - scaffold

must_haves:
  truths:
    - "Running npm run build produces a dist/ directory containing manifest.json, content/index.js, background/index.js, popup/index.html"
    - "Loading the unpacked dist/ directory in chrome://extensions shows the extension as active with no errors"
    - "Opening linkedin.com with the extension installed produces no errors in the chrome://extensions Service Worker console or the page DevTools console (per D-01)"
    - "Folder layout under src/ matches the D-04 spec (content/, background/, popup/, shared/)"
  artifacts:
    - path: "src/manifest.json"
      provides: "MV3 manifest with content_scripts for linkedin.com, service worker, action popup"
      contains: "\"manifest_version\": 3"
    - path: "vite.config.ts"
      provides: "Multi-entry Vite build via vite-plugin-web-extension + @preact/preset-vite"
      contains: "vite-plugin-web-extension"
    - path: "src/content/index.ts"
      provides: "Content script entry stub (logs '[LLB] content script loaded' to console)"
      min_lines: 1
    - path: "src/background/index.ts"
      provides: "Service worker entry stub (chrome.runtime.onMessage listener registered)"
      min_lines: 1
    - path: "tsconfig.json"
      provides: "TypeScript strict mode configuration"
      contains: "\"strict\": true"
    - path: "eslint.config.js"
      provides: "ESLint flat config (ESLint 10+)"
      min_lines: 1
  key_links:
    - from: "src/manifest.json"
      to: "src/content/index.ts"
      via: "content_scripts[0].js path resolved by vite-plugin-web-extension"
      pattern: "content/index"
    - from: "src/manifest.json"
      to: "src/background/index.ts"
      via: "background.service_worker path"
      pattern: "background/index"
    - from: "vite.config.ts"
      to: "src/manifest.json"
      via: "webExtension({ manifest: ... }) plugin call"
      pattern: "manifest"
---

<objective>
Scaffold the Chrome MV3 extension project: install the toolchain (Vite 8 + vite-plugin-web-extension 4.5.1 + TypeScript 6 + Preact 10 + ESLint 10 + Prettier 3), establish the D-04 folder layout under `src/`, write the MV3 manifest targeting linkedin.com, and produce a buildable extension that loads in Chrome without errors.

Purpose: INFRA-01 requires the extension to load on linkedin.com without errors. Every subsequent plan in this phase, and every later phase, depends on this build toolchain and folder layout. No selector or runtime logic is written here — entry files are stubs that print a single loaded log.

Output: A working `npm run build` pipeline producing a loadable unpacked extension; D-04 folder layout established; all stub entry files compile under TypeScript strict mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold project with vite-plugin-web-extension and install dependencies</name>
  <files>package.json, package-lock.json, vite.config.ts, tsconfig.json, .gitignore</files>
  <read_first>
    - CLAUDE.md (project critical constraints — sections "Critical Constraints" and "Architecture")
    - .planning/phases/01-foundation/01-RESEARCH.md (sections: Standard Stack, Package Legitimacy Audit, Code Examples > Vite Config)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-01, D-02, D-03, D-04)
  </read_first>
  <action>
    Run `npm create vite-plugin-web-extension@latest .` in the repository root and select the `vanilla-ts` template per D-01/D-02 (research has verified this template exists in plugin v4.5.1). If the interactive CLI cannot run, use D-03 fallback: `npm create vite@latest . -- --template vanilla-ts` then manually add the plugin.

    After scaffold completes: install runtime dependencies `preact@10` and dev dependencies `vite-plugin-web-extension@^4.5.1 @preact/preset-vite@^2.10 @types/chrome eslint@^10 prettier@^3 typescript@^6`. Do not pin Vite to 5 — the plugin supports Vite 8 (the scaffold default).

    Write `vite.config.ts` so it imports `defineConfig` from `vite`, `preact` from `@preact/preset-vite`, and `webExtension` from `vite-plugin-web-extension`, registers both plugins, and points `webExtension({ manifest: ... })` at `src/manifest.json`. The build output directory must be `dist/`.

    Write `tsconfig.json` with `"strict": true`, `"target": "ES2022"`, `"module": "ESNext"`, `"moduleResolution": "bundler"`, `"types": ["chrome"]`, `"jsx": "preserve"`, `"jsxImportSource": "preact"`, `"noUncheckedIndexedAccess": true`, `"include": ["src/**/*"]`.

    Add `.gitignore` entries: `node_modules`, `dist`, `.DS_Store`, `*.log`.

    Do NOT yet write source entry files (next task). Do NOT introduce any LinkedIn selector — selectors live only in `src/content/selectors.ts` per CLAUDE.md constraint #1 and INFRA-04.
  </action>
  <verify>
    <automated>npm install --no-audit --no-fund &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `devDependencies` contains `vite-plugin-web-extension`, `@preact/preset-vite`, `@types/chrome`, `eslint`, `prettier`, `typescript`
    - `package.json` `dependencies` contains `preact`
    - `vite.config.ts` imports `webExtension` from `vite-plugin-web-extension` and calls it within the `plugins` array
    - `vite.config.ts` imports `preact` from `@preact/preset-vite` and calls it within the `plugins` array
    - `tsconfig.json` contains the string `"strict": true`
    - `tsconfig.json` `compilerOptions.types` array includes `"chrome"`
    - `npx tsc --noEmit` exits 0
    - `.gitignore` contains `node_modules` and `dist`
  </acceptance_criteria>
  <done>Toolchain installed, TypeScript strict mode active, Vite config wired to the web extension plugin and Preact preset, scaffold runs `npx tsc --noEmit` cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Establish D-04 folder layout, write MV3 manifest and stub entry files</name>
  <files>src/manifest.json, src/content/index.ts, src/background/index.ts, src/popup/index.html, src/popup/index.tsx, eslint.config.js, .prettierrc</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (decision D-04 folder structure)
    - .planning/phases/01-foundation/01-RESEARCH.md (sections: Code Examples > Manifest, Recommended Project Structure)
    - CLAUDE.md (constraint #4 service worker stateless)
    - vite.config.ts (verify manifest path resolves)
  </read_first>
  <action>
    Create the D-04 folder layout under `src/`: `src/content/`, `src/background/`, `src/popup/`, `src/shared/` (empty for now; populated by plan 03). Delete any stray template files generated by the scaffolder that fall outside this layout (e.g., a default `src/main.ts` or `src/counter.ts` from `vanilla-ts`).

    Write `src/manifest.json` with: `manifest_version: 3`, `name: "LinkedIn Blocker"`, `version: "0.1.0"`, `description: "Hides AI-generated posts on LinkedIn."`, `permissions: ["storage", "activeTab"]`, `host_permissions: ["https://www.linkedin.com/*"]`, `background: { service_worker: "background/index.js", type: "module" }`, `content_scripts: [{ matches: ["https://www.linkedin.com/*"], js: ["content/index.js"], run_at: "document_idle", all_frames: false }]`, `action: { default_popup: "popup/index.html", default_title: "LinkedIn Blocker" }`.

    Write `src/content/index.ts` as a one-line stub that logs `[LLB] content script loaded on <location.href>`. Do not import any selector module yet (selectors do not exist; created in plan 03).

    Write `src/background/index.ts` as a stub that registers `chrome.runtime.onMessage.addListener` returning `false` (no-op handler). Add a top-level `console.log('[LLB] service worker started')`. No module-scope mutable state per CLAUDE.md constraint #4.

    Write `src/popup/index.html` as a minimal HTML shell with a `<div id="root"></div>` and a `<script type="module" src="./index.tsx">`. Write `src/popup/index.tsx` rendering a single Preact element `<div>LinkedIn Blocker</div>` into `#root` using `render` from `preact`. This is a stub; the real popup is built in Phase 4.

    Write `eslint.config.js` as an ESLint 10 flat config exporting an array with: `files: ["src/**/*.{ts,tsx}"]`, parser TypeScript ESLint, and a rule banning `eval` (`"no-eval": "error"`). Write `.prettierrc` with `{ "semi": true, "singleQuote": true, "trailingComma": "all", "printWidth": 100 }`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f dist/manifest.json &amp;&amp; test -f dist/content/index.js &amp;&amp; test -f dist/background/index.js &amp;&amp; test -f dist/popup/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `src/manifest.json` parses as valid JSON and contains `"manifest_version": 3`
    - `src/manifest.json` `host_permissions` array contains `"https://www.linkedin.com/*"`
    - `src/manifest.json` `content_scripts[0].matches` contains `"https://www.linkedin.com/*"` and `content_scripts[0].run_at` equals `"document_idle"`
    - `src/manifest.json` `background.service_worker` equals `"background/index.js"` and `background.type` equals `"module"`
    - `src/content/index.ts` exists and contains the substring `[LLB] content script loaded`
    - `src/background/index.ts` exists and contains `chrome.runtime.onMessage.addListener`
    - `src/popup/index.tsx` imports from `preact`
    - `npm run build` exits 0
    - `dist/manifest.json`, `dist/content/index.js`, `dist/background/index.js`, `dist/popup/index.html` all exist after build
    - `eslint.config.js` exports an array (flat config) and includes a `"no-eval"` rule
  </acceptance_criteria>
  <done>D-04 folder structure exists; manifest targets linkedin.com; stub entries compile; `npm run build` produces a loadable unpacked extension under `dist/`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies extension loads on linkedin.com without errors (INFRA-01)</name>
  <what-built>A buildable Chrome MV3 extension with manifest targeting linkedin.com, content script stub, service worker stub, and popup stub. Output in `dist/`.</what-built>
  <how-to-verify>
    1. Run `npm run build` and confirm `dist/` is populated.
    2. Open Chrome and navigate to `chrome://extensions`.
    3. Enable Developer mode (top right).
    4. Click "Load unpacked" and select the `dist/` directory.
    5. Confirm the "LinkedIn Blocker" card appears with no red "Errors" button.
    6. Click "Inspect views: service worker" — the DevTools console must show `[LLB] service worker started` and no errors.
    7. Navigate to `https://www.linkedin.com/feed/` (logged in).
    8. Open the page DevTools (F12) → Console. Confirm a line containing `[LLB] content script loaded` appears, and no extension-related errors are present.
    9. Click the extension icon — the popup must open and show "LinkedIn Blocker" text without errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 steps pass, or describe any errors seen.</resume-signal>
  <acceptance_criteria>
    - Extension card in chrome://extensions shows no error button
    - Service worker console contains `[LLB] service worker started` with no errors
    - linkedin.com page console contains `[LLB] content script loaded` with no errors
    - Popup opens without errors when icon clicked
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LinkedIn page → content script | Untrusted DOM data crosses isolated-world boundary when the content script reads `document` |
| Content script → service worker | `chrome.runtime.sendMessage` boundary (no traffic in Phase 1 but listener registered) |
| User filesystem → Chrome | `chrome://extensions` "Load unpacked" reads dist/ — trusted but supply-chain risk on `node_modules` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | manifest.json host_permissions | mitigate | Restrict to `https://www.linkedin.com/*` only — no `<all_urls>`, no wildcard schemes |
| T-01-02 | Elevation of Privilege | Content script world | mitigate | Use ISOLATED world (MV3 default — do NOT add `"world": "MAIN"` to content_scripts) |
| T-01-03 | Tampering | MV3 CSP / eval | mitigate | ESLint `no-eval: error` rule; bundled static modules only; no `new Function` |
| T-01-04 | Information Disclosure | Service worker state | mitigate | Service worker stub has no module-scope mutable state; future state goes to chrome.storage.local per CLAUDE.md constraint #4 |
| T-01-SC | Tampering | npm install (supply chain) | mitigate | All packages [VERIFIED] in research §Package Legitimacy Audit; none flagged [ASSUMED] or [SUS]; legitimacy gate not required |
| T-01-05 | Information Disclosure | activeTab permission | accept | activeTab grants temporary host access on user gesture; lower risk than persistent broad host permission; revisit in Phase 5 when block deep-link opens |
</threat_model>

<verification>
- Extension loads as MV3 in chrome://extensions without errors
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
- Stub console logs appear in both service worker and content script contexts on linkedin.com
- Folder layout matches D-04 exactly
</verification>

<success_criteria>
INFRA-01 satisfied: extension loads as Chrome MV3 on linkedin.com without errors. Toolchain ready for plans 03 and 04 to add selector registry and observer logic on top.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done. Include: scaffold method used (template path vs. D-03 fallback), final package versions installed, any structural deviations from D-04 and why, and confirmation that the human verification checkpoint passed.
</output>
