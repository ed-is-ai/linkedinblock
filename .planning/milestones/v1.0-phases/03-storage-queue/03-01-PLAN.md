---
phase: 03-storage-queue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/content/index.ts
autonomous: true
requirements: [QUEUE-01, QUEUE-02]

must_haves:
  truths:
    - "FlaggedAccountStub is renamed to FlaggedAccount with two new fields: postCount (number) and peakScore (number)"
    - "StorageSchema gains dismissedAccounts?: string[] as a separate key from flaggedAccounts"
    - "StorageSchema.flaggedAccounts types to Record<string, FlaggedAccount> (not FlaggedAccountStub)"
    - "src/content/index.ts import is updated from FlaggedAccountStub to FlaggedAccount; the type stub literal is updated to match the new shape"
    - "npx tsc --noEmit exits 0 after this plan"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "Expanded FlaggedAccount type + dismissedAccounts in StorageSchema"
      contains: "FlaggedAccount"
    - path: "src/content/index.ts"
      provides: "Updated import to use FlaggedAccount instead of FlaggedAccountStub"
      contains: "FlaggedAccount"
  key_links:
    - from: "src/content/index.ts"
      to: "src/shared/types.ts"
      via: "import type { ..., FlaggedAccount } from '../shared/types'"
      pattern: "import type \\{ .* FlaggedAccount"
---

<objective>
Expand the shared type contract to match Phase 3's storage decisions.

`FlaggedAccountStub` → `FlaggedAccount`: the Phase 2 stub shape grows two new numeric fields (`postCount`, `peakScore`) to support the EMA rolling score strategy decided in 03-CONTEXT.md D-01/D-02/D-03. The stub name was always a placeholder — Phase 3 is where the full account record is defined.

`StorageSchema` gains `dismissedAccounts?: string[]` as a separate key (D-14 from 03-CONTEXT.md). Phase 3 declares it empty; Phase 5 writes to it. Declaring it now means the typed `storageGet` / `storageSet` wrappers accept the key without `as any` casts.

This plan touches only types — no runtime logic changes. Plans 02 and 03 can then reference `FlaggedAccount` without invention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-storage-queue/03-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename FlaggedAccountStub → FlaggedAccount; add postCount + peakScore fields</name>
  <files>src/shared/types.ts</files>
  <read_first>
    - src/shared/types.ts (entire file — the FlaggedAccountStub interface is the target; read the JSDoc style and the Phase 2 foreshadowing comment at line 73)
    - .planning/phases/03-storage-queue/03-CONTEXT.md §D-01, §D-02, §D-03, §D-05, §D-06 (rolling score strategy, new fields, status field behaviour)
  </read_first>
  <action>
    In src/shared/types.ts:

    1. Rename the export `interface FlaggedAccountStub` to `export interface FlaggedAccount`. Update the JSDoc header to reflect that this is the Phase 3 full account record (no longer a stub), and note that Phase 5 will expand the `status` union.

    2. Add two new required fields to the interface body (insert after `compositeScore`):
       - `postCount: number` — JSDoc: "Number of posts from this account that scored >= 35 and were persisted. Used as context for the EMA rolling score."
       - `peakScore: number` — JSDoc: "Highest single-post composite score ever recorded for this account (0–100). Popup Phase 4 may sort by this rather than the EMA compositeScore."

    3. Update the `compositeScore` JSDoc to reflect its new meaning: "EMA rolling average composite score (0–100). Updated each time a new post from this account is persisted: score = score × (1 - EMA_ALPHA) + newScore × EMA_ALPHA. Phase 2 set this to peak; Phase 3 changes the semantics to EMA."

    4. Leave `status: 'pending'` unchanged — Phase 5 expands the union. Update the JSDoc comment on `status` to explicitly state: "Phase 5 will expand this to 'pending' | 'blocked' | 'dismissed'. Phase 3 only ever writes 'pending'."

    5. Update `StorageSchema.flaggedAccounts` to type as `Record<string, FlaggedAccount>` (was `Record<string, FlaggedAccountStub>`). Update the JSDoc accordingly.

    6. Add `dismissedAccounts?: string[]` to `StorageSchema`. JSDoc: "Array of authorId strings that have been dismissed as false positives (Phase 5 writes). Phase 3 declares the key with an empty-array default; content script loads it as a Set<string> for O(1) lookup."

    7. Do NOT touch `PostData`, `DetectionResult`, `Detector`, or `ObservedPost` interfaces.
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('src/shared/types.ts','utf8'); const checks = [['interface FlaggedAccount',true],['interface FlaggedAccountStub',false],['postCount: number',true],['peakScore: number',true],['dismissedAccounts',true],['Record<string, FlaggedAccount>',true],['Record<string, FlaggedAccountStub>',false]]; const fail = checks.filter(([pat,want]) => s.includes(pat) !== want); if (fail.length) { console.error('FAIL', fail.map(([p])=>p)); process.exit(1); } console.log('types.ts checks pass');" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `interface FlaggedAccountStub` no longer exists in types.ts
    - `interface FlaggedAccount` is exported with all original fields plus `postCount: number` and `peakScore: number`
    - `StorageSchema.flaggedAccounts` is typed as `Record<string, FlaggedAccount>`
    - `StorageSchema` has `dismissedAccounts?: string[]`
    - `npx tsc --noEmit` exits 0 (import sites in content/index.ts will be fixed in Task 2)
  </acceptance_criteria>
  <done>FlaggedAccount type is correct shape; StorageSchema has dismissedAccounts; TypeScript clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update content/index.ts import — FlaggedAccountStub → FlaggedAccount</name>
  <files>src/content/index.ts</files>
  <read_first>
    - src/content/index.ts lines 1–15 (the import block; look for `FlaggedAccountStub`)
    - src/shared/types.ts (after Task 1 — confirm `FlaggedAccount` is the exported name)
  </read_first>
  <action>
    In src/content/index.ts:

    1. In the import statement on line 9, replace `FlaggedAccountStub` with `FlaggedAccount`.

    2. Find the `const stub: FlaggedAccountStub = {` object literal inside `persistFlaggedAccount` (currently around line 81). Change the type annotation to `FlaggedAccount`.

    3. Add the two new required fields to that object literal so it satisfies the expanded interface:
       - `postCount: 1` (first post from this account)
       - `peakScore: compositeScore` (first post; peak = current score)

    4. Do NOT change any other logic in `persistFlaggedAccount` — the EMA rewrite happens in Plan 03. The existing `compositeScore = Math.max(...)` path for existing entries remains as-is for now.

    5. Verify the file compiles after the change.
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('src/content/index.ts','utf8'); if (s.includes('FlaggedAccountStub')) { console.error('FlaggedAccountStub still present'); process.exit(1); } if (!s.includes('FlaggedAccount')) { console.error('FlaggedAccount import missing'); process.exit(1); } if (!s.includes('postCount: 1')) { console.error('postCount missing from new entry literal'); process.exit(1); } if (!s.includes('peakScore: compositeScore')) { console.error('peakScore missing from new entry literal'); process.exit(1); } console.log('index.ts ok');" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `FlaggedAccountStub` does not appear anywhere in content/index.ts
    - The import block imports `FlaggedAccount` from '../shared/types'
    - The new-entry object literal in `persistFlaggedAccount` includes `postCount: 1` and `peakScore: compositeScore`
    - `npx tsc --noEmit` exits 0 across the whole project
  </acceptance_criteria>
  <done>content/index.ts compiles against FlaggedAccount; new fields present on new-entry path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local read | Existing entries may lack postCount/peakScore (Phase 2 data); Plan 03 handles migration at read time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | FlaggedAccount schema drift | mitigate | TypeScript strict mode catches missing fields at compile time; no runtime schema validation needed for internal writes |
| T-03-02 | Information Disclosure | dismissedAccounts key declared | accept | Contains only authorId strings (profile slugs); no personally identifying content beyond what LinkedIn exposes |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 with no errors
- `grep -rn "FlaggedAccountStub" src/` returns no results
- `grep -rn "FlaggedAccount" src/` returns hits in types.ts and content/index.ts only
- `grep -n "dismissedAccounts" src/shared/types.ts` returns at least one match
</verification>

<success_criteria>
- All source files compile under strict mode
- `FlaggedAccount` exports `postCount` and `peakScore` as required fields
- `StorageSchema.dismissedAccounts` is declared and typed
- No other files are modified in this plan
</success_criteria>

<output>
Create `.planning/phases/03-storage-queue/03-01-SUMMARY.md` when done summarising:
- FlaggedAccountStub renamed to FlaggedAccount; postCount + peakScore added
- StorageSchema.dismissedAccounts declared (Phase 5 writes; Phase 3 declares)
- content/index.ts new-entry literal updated to satisfy expanded interface
- EMA logic is NOT yet implemented — that is Plan 03
</output>
