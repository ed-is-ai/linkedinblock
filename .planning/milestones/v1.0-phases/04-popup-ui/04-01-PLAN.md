---
phase: 04-popup-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/popup/AccountRow.tsx
autonomous: true
requirements: [POPUP-02]

must_haves:
  truths:
    - "src/popup/AccountRow.tsx exists and exports a default AccountRow component"
    - "AccountRow accepts a single prop: account of type FlaggedAccount"
    - "AccountRow displays authorName as a clickable link to authorProfileUrl"
    - "AccountRow displays peakScore as the headline number and compositeScore as secondary"
    - "AccountRow displays postCount with proper singular/plural"
    - "AccountRow displays signal chips (up to 4, then +N more) from account.signals"
    - "npx tsc --noEmit exits 0 after this plan"
  artifacts:
    - path: "src/popup/AccountRow.tsx"
      provides: "Pure presentational component for a single flagged account row"
      contains: "AccountRow"
  key_links:
    - from: "src/popup/AccountRow.tsx"
      to: "src/shared/types.ts"
      via: "import type { FlaggedAccount } from '../shared/types'"
      pattern: "from ['\\\"].*shared/types['\\\"]"
---

<objective>
Create `src/popup/AccountRow.tsx` — a pure presentational Preact component for a single flagged account row. No chrome API calls. No side effects. Props in, JSX out.

This component is the visual core of the popup queue. Plan 02 imports it into the main `App` component. By isolating it here, Plan 02 can focus purely on storage wiring.

Row layout (left-to-right, top-to-bottom):
```
┌─────────────────────────────────┐
│ [Author Name ↗]    Peak: 82     │
│ avg: 74 • 3 posts               │
│ [listicle] [buzzwords] [cta] +2 │
└─────────────────────────────────┘
```

Signal chips show the keys from `account.signals` where value > 0, up to 4 chips, then `+N more` static text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-popup-ui/04-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/popup/AccountRow.tsx — presentational account row component</name>
  <files>src/popup/AccountRow.tsx</files>
  <read_first>
    - src/shared/types.ts (read the FlaggedAccount interface — all fields available as props)
    - src/popup/index.tsx (read the existing inline styles convention and Preact import style)
    - .planning/phases/04-popup-ui/04-CONTEXT.md §D-04, §D-05, §D-06, §D-12, §D-13 (component decisions, signal chips, score display, styling)
  </read_first>
  <action>
    Create `src/popup/AccountRow.tsx`. This file must:

    **1. Imports:**
    ```typescript
    import type { FlaggedAccount } from '../shared/types';
    import type { JSX } from 'preact';
    ```
    No `preact/hooks` needed — this is a pure functional component with no state.

    **2. Props interface:**
    ```typescript
    interface AccountRowProps {
      account: FlaggedAccount;
    }
    ```

    **3. Signal chips helper:**
    Inside the component body (not exported), compute the chips array:
    ```typescript
    const activeSignals = Object.entries(account.signals)
      .filter(([, v]) => v > 0)
      .map(([k]) => k);
    const visibleChips = activeSignals.slice(0, 4);
    const hiddenCount = activeSignals.length - visibleChips.length;
    ```

    **4. JSX layout:**
    The component returns a `<div>` row structured as:

    Row top line (flexbox, space-between):
    - Left: author name as an `<a href={account.authorProfileUrl} target="_blank" rel="noreferrer">` with `style={rowStyles.nameLink}`. Truncate with `overflow: hidden, textOverflow: 'ellipsis', whiteSpace: 'nowrap', maxWidth: 160`.
    - Right: `<span style={rowStyles.peakScore}>Peak: {account.peakScore}</span>` (large score, LinkedIn blue)

    Row second line (flex, gap 6):
    - `avg: {Math.round(account.compositeScore)}` in muted colour
    - Bullet separator `•`
    - `{account.postCount} {account.postCount === 1 ? 'post' : 'posts'}`

    Row chip line (flex, wrap, gap 4, marginTop 4):
    - For each `chip` in `visibleChips`: `<span style={rowStyles.chip}>{chip}</span>`
    - If `hiddenCount > 0`: `<span style={rowStyles.moreChips}>+{hiddenCount} more</span>`

    **5. Styles (all inline, as a `Record<string, JSX.CSSProperties>` constant):**
    ```
    row: {
      padding: '10px 0',
      borderBottom: '1px solid #e5e7eb',
    }
    nameLink: {
      color: '#1a1a1a',
      fontWeight: 600,
      fontSize: 13,
      textDecoration: 'none',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
      maxWidth: 160,
      display: 'block',
    }
    peakScore: {
      color: '#0a66c2',
      fontWeight: 700,
      fontSize: 13,
      whiteSpace: 'nowrap',
    }
    meta: {
      fontSize: 11,
      color: '#6b7280',
      marginTop: 2,
      display: 'flex',
      gap: 4,
      alignItems: 'center',
    }
    chipRow: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 4,
      marginTop: 4,
    }
    chip: {
      background: '#f3f4f6',
      color: '#374151',
      fontSize: 10,
      padding: '2px 6px',
      borderRadius: 10,
    }
    moreChips: {
      fontSize: 10,
      color: '#9ca3af',
      alignSelf: 'center',
    }
    ```
    Note: use `as const satisfies Record<string, JSX.CSSProperties>` or `as Record<string, JSX.CSSProperties>` for the type annotation, consistent with the existing popup file.

    **6. Default export:**
    ```typescript
    export default function AccountRow({ account }: AccountRowProps): JSX.Element { ... }
    ```

    **7. Constraints:**
    - No `chrome.` calls in this file
    - No `useEffect`, `useState`, or any hook
    - No `document.` access
    - Signal key names are already human-readable (e.g. `'listicle'`, `'buzzwords'`, `'headline-formula'`) — display them as-is, no transformation
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('src/popup/AccountRow.tsx','utf8'); const need = ['FlaggedAccount','AccountRow','authorProfileUrl','peakScore','compositeScore','postCount','visibleChips','hiddenCount','chip']; const miss = need.filter(n => !s.includes(n)); if (miss.length) { console.error('missing:', miss); process.exit(1); } if (s.includes('chrome.') || s.includes('useEffect') || s.includes('useState')) { console.error('no chrome/hooks allowed in AccountRow'); process.exit(1); } console.log('AccountRow.tsx ok');" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/popup/AccountRow.tsx` exists with a default export `AccountRow`
    - Accepts `{ account: FlaggedAccount }` as its only prop
    - Renders author name as an anchor to `authorProfileUrl` opening in a new tab
    - Shows `peakScore` in LinkedIn blue (#0a66c2)
    - Shows `Math.round(compositeScore)` as the EMA average
    - Shows `postCount` with proper singular/plural (`1 post`, `3 posts`)
    - Signal chips rendered from `account.signals` keys where value > 0
    - At most 4 chips shown; remainder shown as `+N more`
    - No chrome API calls, no hooks, no document access
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AccountRow.tsx is a pure presentational component; TypeScript clean; ready for Plan 02 import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FlaggedAccount data from storage | authorName/authorProfileUrl come from LinkedIn DOM (untrusted). Rendered as text content and href attribute only — no innerHTML, no eval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | XSS | authorName in JSX | mitigate | Preact escapes text content by default; JSX renders authorName as text child, not innerHTML |
| T-04-02 | Open Redirect | authorProfileUrl used as href | mitigate | Value comes from `anchor.href` (browser-parsed LinkedIn URL) in content script; `rel="noreferrer"` prevents tab access to opener |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `grep -n "chrome\." src/popup/AccountRow.tsx` returns no results
- `grep -n "innerHTML" src/popup/AccountRow.tsx` returns no results
</verification>

<success_criteria>
- AccountRow.tsx compiles under strict mode
- Component is purely presentational — no side effects, no chrome API, no hooks
- Signal chips correctly implement the 4-chip cap with `+N more` overflow
- Plan 02 can import and use this component without modification
</success_criteria>

<output>
Create `.planning/phases/04-popup-ui/04-01-SUMMARY.md` when done summarising:
- AccountRow component created with score display, signal chips, author link
- TypeScript compilation result
- Any issues or deviations
</output>
