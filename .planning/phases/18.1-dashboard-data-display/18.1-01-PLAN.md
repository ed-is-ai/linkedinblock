---
phase: 18.1-dashboard-data-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/index.tsx
  - mockup-dashboard.html
autonomous: false
requirements: [DASH-DISPLAY-01]
must_haves:
  truths:
    - "npm run type-check passes with zero TypeScript errors"
    - "npm run build produces a dist/ bundle without errors"
    - "The throwaway mockup-dashboard.html no longer exists in the repo"
    - "Dashboard shows the two visual bar-chart rows (posts flagged, bot profiles) instead of plain-text percentages"
    - "Dashboard shows the 'Net AI voice posts in feed' SVG line chart card"
    - "The old 'Signal categories' card (AI language / Bot behaviour bars) is gone"
  artifacts:
    - path: "src/dashboard/index.tsx"
      provides: "Dashboard UI with bar-chart stat rows and NetPostsChart SVG line chart"
      contains: "NetPostsChart"
  key_links:
    - from: "src/dashboard/index.tsx"
      to: "chrome.storage.local"
      via: "chrome.storage.local.get(['flaggedAccounts','dailyStats','storedPosts','dismissedAccounts'])"
      pattern: "chrome\\.storage\\.local\\.get"
    - from: "NetPostsChart"
      to: "DailyStats"
      via: "(seen - hidden) / seen * 100 per day"
      pattern: "seen - .*hidden"
---

<objective>
Finalize the dashboard data-display UX improvements that were drafted in the current session. The Preact code in `src/dashboard/index.tsx` is already written (signal-categories card removed, plain-text stats replaced with horizontal bar-chart rows, a new "Net AI voice posts in feed" SVG line-chart card added). This plan closes out the work: fix the one outstanding TypeScript error blocking the build, delete the throwaway mockup file, and confirm the visual result through a human checkpoint.

Purpose: The display code is done but not yet build-clean or shipped. A single broken type guard (`tsc` error at index.tsx:41) blocks `npm run build`, and a temporary `mockup-dashboard.html` is sitting in the project root.

Output: A build-clean dashboard, no stray mockup file, and human-verified visual output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@src/dashboard/index.tsx
@src/shared/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the TypeScript error in the 30-day x-axis label code</name>
  <read_first>
    - src/dashboard/index.tsx (lines 1-62 — the NetPostsChart component)
    - src/shared/types.ts (DailyStats interface)
    - tsconfig.json (confirm noUncheckedIndexedAccess is the cause)
  </read_first>
  <action>
    Running `npm run type-check` currently fails with exactly one error:
    `src/dashboard/index.tsx(41,51): error TS2532: Object is possibly 'undefined'.`

    The error is on the 30-day x-axis label expression at line 41:
    `[0, 7, 14, 21, n - 1].map(i => ({ i, label: days[i].slice(5) }))`.
    Under `noUncheckedIndexedAccess`, `days[i]` is typed `string | undefined`, so `.slice(5)` is unsafe.

    Fix it by guarding the index access — derive the label from a locally-bound `const d = days[i]` and fall back to an empty string when undefined (e.g. `days[i]?.slice(5) ?? ''`). Do NOT change the chart math, the 7-day branch behavior, or remove the `noUncheckedIndexedAccess` compiler option (that option is a project-wide safety setting — fix the call site, not the config). Keep the existing label set `[0, 7, 14, 21, n - 1]` intact.

    After editing, run `npm run type-check` and confirm zero errors. Then run `npm run build` and confirm it produces a `dist/` bundle without errors.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0 with no TS errors
    - `npm run build` completes and writes the dashboard bundle into dist/ without errors
    - The `noUncheckedIndexedAccess` option in tsconfig.json is unchanged
    - The 7-day label branch and chart math (xOf/yOf/values) are unchanged
  </acceptance_criteria>
  <done>The dashboard compiles and builds cleanly; the index.tsx:41 TS2532 error is resolved at the call site.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the throwaway mockup file</name>
  <read_first>
    - mockup-dashboard.html (confirm it is the standalone HTML mockup, not a referenced build input)
    - vite.config.ts (confirm mockup-dashboard.html is NOT listed as a Vite entry/input)
  </read_first>
  <action>
    Delete `mockup-dashboard.html` from the project root. Before deleting, confirm via `vite.config.ts` and a repo-wide search that nothing imports, references, or lists it as a build entry — it was a throwaway design mockup created this session and must not ship. If any reference is found (it should not be), stop and report instead of deleting.
  </action>
  <verify>
    <automated>test ! -f mockup-dashboard.html && echo OK</automated>
    <automated>grep -rIl "mockup-dashboard" src vite.config.ts package.json 2>/dev/null | grep -v '^$' && echo "REFERENCES FOUND" || echo "NO REFERENCES"</automated>
  </verify>
  <acceptance_criteria>
    - `mockup-dashboard.html` no longer exists in the project root
    - No source file, vite.config.ts, or package.json references `mockup-dashboard`
    - `npm run build` still succeeds after deletion
  </acceptance_criteria>
  <done>The mockup file is removed and no build input or source code depended on it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The dashboard (chrome options page) now renders:
    1. A "Posts hidden — all time" card with two visual horizontal bar rows: "Posts flagged — last N days" (blue bar) and "Bot profiles seen — last N days" (amber bar), replacing the old plain-text percentages.
    2. A new "Net AI voice posts in feed — last N days" card with an inline SVG line chart plotting `(seen - hidden) / seen * 100` per day.
    3. The old "Signal categories" (AI language / Bot behaviour) card has been removed.
    The 7-day / 30-day toggle still switches both the bar windows and the chart x-axis.
  </what-built>
  <how-to-verify>
    1. Run `npm run build` (already verified in Task 1).
    2. Load the unpacked extension: open chrome://extensions, enable Developer mode, "Load unpacked", select the `dist/` folder (or reload it if already loaded).
    3. Open the dashboard via the extension's options page (right-click the extension icon -> Options, or chrome://extensions -> Details -> Extension options).
    4. Confirm visually:
       - The two stat rows render as horizontal bars (blue + amber), not plain text percentages.
       - The "Net AI voice posts in feed" card shows an SVG line chart (or the "No feed data yet" message if storage is empty).
       - There is NO "Signal categories" card.
       - Toggling 7 days / 30 days updates both the bar labels and the chart x-axis labels.
    5. (Optional) If you have stored dailyStats, confirm the line chart plots sensible percentages and the 30-day view shows x-axis labels without crashing.
  </how-to-verify>
  <resume-signal>Type "approved" if the dashboard renders as described, or describe what looks wrong.</resume-signal>
</task>

</tasks>

<verification>
- `npm run type-check` passes with zero errors
- `npm run build` produces a dist/ bundle
- `mockup-dashboard.html` is deleted
- Human confirms the three visual changes render correctly in the loaded extension
</verification>

<success_criteria>
- Dashboard compiles and builds clean
- Throwaway mockup removed
- Bar-chart stat rows, Net AI voice posts line chart present; Signal categories card absent
- Human verification approved
</success_criteria>

<output>
Create `.planning/phases/18.1-dashboard-data-display/18.1-01-SUMMARY.md` when done
</output>
