---
phase: 11-posts-export
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/dashboard/index.tsx
autonomous: true
requirements: [EXPORT-03]

must_haves:
  truths:
    - "buildPostsCsvExport is imported from ./dataManagement in dashboard/index.tsx"
    - "handleExportPostsCsv handler calls buildPostsCsvExport(posts) and triggers a download named linkedin-blocker-posts-YYYY-MM-DD.csv"
    - "An 'Export Posts CSV' button appears in the Data management card alongside the existing Export JSON and Export CSV buttons"
    - "The button is disabled (or not rendered) when posts.length === 0, consistent with the existing export button pattern"
    - "npx tsc --noEmit exits 0"
  artifacts:
    - path: "src/dashboard/index.tsx"
      provides: "Export Posts CSV button wired to buildPostsCsvExport"
      contains: "handleExportPostsCsv"
---

<objective>
Wire the "Export Posts CSV" button into the dashboard. `buildPostsCsvExport` was added in Plan 01; this plan imports it and adds a handler + button.

The button lives in the "Data management" card alongside the existing "Export JSON" and "Export CSV" buttons. It downloads `linkedin-blocker-posts-YYYY-MM-DD.csv`.

Pattern to follow: identical to `handleExportCsv` — use `triggerDownload(content, filename, 'text/csv')`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-posts-export/11-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Import buildPostsCsvExport, add handler and button to dashboard</name>
  <files>src/dashboard/index.tsx</files>
  <read_first>
    - src/dashboard/index.tsx (full file — understand the existing import line for dataManagement, the triggerDownload helper, handleExportCsv pattern, and the export button group in the JSX)
    - src/dashboard/dataManagement.ts (confirm buildPostsCsvExport signature and that it's exported)
  </read_first>
  <action>
    **1. Extend the dataManagement import** to include `buildPostsCsvExport`:
    ```typescript
    import { buildJsonExport, buildCsvExport, buildPostsCsvExport, deriveCleanseCount, filterCleansed } from './dataManagement';
    ```

    **2. Add a handler** — directly after `handleExportCsv`, add:
    ```typescript
    function handleExportPostsCsv(): void {
      const today = new Date().toISOString().slice(0, 10);
      triggerDownload(buildPostsCsvExport(posts), `linkedin-blocker-posts-${today}.csv`, 'text/csv');
    }
    ```

    **3. Add the button** — in the export button group (the `<div style={{ display: 'flex', gap: 8, marginTop: 8 }}>` that contains Export JSON and Export CSV), add a third button:
    ```jsx
    <button style={s.actionBtn} onClick={handleExportPostsCsv}>Export Posts CSV</button>
    ```

    Place it after the "Export CSV" button. The surrounding guard (`accounts.length === 0` → show placeholder) already applies to the entire group, so the new button shows only when accounts exist. That is acceptable — posts are associated with accounts so if there are no accounts, there are no posts to export.

    No new styles needed.
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('src/dashboard/index.tsx','utf8'); const need = ['buildPostsCsvExport','handleExportPostsCsv','Export Posts CSV','linkedin-blocker-posts-']; const miss = need.filter(n => !s.includes(n)); if (miss.length) { console.error('missing:', miss); process.exit(1); } console.log('ok');" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `buildPostsCsvExport` is imported from `./dataManagement`
    - `handleExportPostsCsv` uses `triggerDownload` with filename `linkedin-blocker-posts-YYYY-MM-DD.csv`
    - "Export Posts CSV" button is present in the export section of the Data management card
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Export Posts CSV button wired; TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-11-02 | Data exposure | Posts CSV download | accept | Extension downloads to user's own machine. User explicitly initiates download. Content is what the user's own extension already has access to in chrome.storage.local. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- Dashboard renders without errors
- "Export Posts CSV" button appears in the Data management card
- Clicking it triggers a CSV download with filename `linkedin-blocker-posts-YYYY-MM-DD.csv`
</verification>

<success_criteria>
- EXPORT-03 fully satisfied: the CSV download is available from the dashboard
- No regression to existing Export JSON, Export CSV, or Cleanse functionality
</success_criteria>

<output>
Create `.planning/phases/11-posts-export/11-02-SUMMARY.md` when done.
</output>
