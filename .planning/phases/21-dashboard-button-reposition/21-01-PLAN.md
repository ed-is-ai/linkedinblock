---
phase: 21-dashboard-button-reposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/popup/index.tsx]
autonomous: true
requirements: [POPUP-04, POPUP-05]

must_haves:
  truths:
    - "Opening the popup shows the 📊 View Dashboard button in the header region, above the feed-health line and the pending account list, without opening Settings (POPUP-04)"
    - "Clicking the relocated button opens dashboard/index.html in a new tab — identical behavior to the prior in-settings button"
    - "The ⚙ Settings disclosure no longer contains a View Dashboard button; it retains only the threshold slider, mode/API-key section, and Save/Clear controls (POPUP-05)"
    - "The View Dashboard button is moved, not duplicated — exactly one openDashboard call site remains"
    - "The change is confined to src/popup/index.tsx; no other files are modified (ROADMAP SC#4)"
  artifacts:
    - path: "src/popup/index.tsx"
      provides: "Relocated 📊 View Dashboard button rendered between the styles.header block and the feedPct paragraph, reusing styles.dashboardLink"
      contains: "styles.dashboardLink"
  key_links:
    - from: "src/popup/index.tsx header region button"
      to: "openDashboard()"
      via: "onClick handler"
      pattern: "onClick=\\{openDashboard\\}"
    - from: "relocated button"
      to: "styles.dashboardLink"
      via: "style prop reuse"
      pattern: "styles\\.dashboardLink"
---

<objective>
Relocate the `📊 View Dashboard` button out of the ⚙ Settings disclosure into the popup header region — rendered directly below the `<div style={styles.header}>` title/badge row and before the `feedPct` feed-health line — so the dashboard is discoverable on popup open without any interaction. Remove the in-settings copy (moved, not duplicated). No behavior change.

Purpose: Surface the dashboard action at the top of the popup (POPUP-04), and declutter Settings down to threshold + API key + export/cleanse controls (POPUP-05).
Output: Modified `src/popup/index.tsx` — single file, single cohesive change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/21-dashboard-button-reposition/21-CONTEXT.md
@.planning/phases/21-dashboard-button-reposition/21-UI-SPEC.md
@src/popup/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the View Dashboard button to the popup header region and remove the in-settings copy</name>
  <files>src/popup/index.tsx</files>
  <read_first>
    - src/popup/index.tsx — read the LIVE file first; line anchors below are approximate. Locate: the `openDashboard()` handler (~line 101), the `<div style={styles.header}>` title/badge block (~lines 137-142), the `feedPct` paragraph (~lines 144-146), the in-settings `<button onClick={openDashboard} style={styles.dashboardLink}>📊 View Dashboard</button>` inside the `<details>` Settings body (~lines 219-221), the following `<hr style={styles.divider} />` (~line 223), and the `styles.dashboardLink` definition (~line 361).
    - .planning/phases/21-dashboard-button-reposition/21-UI-SPEC.md — Component Inventory (relocated + deleted element), Layout Structure (Post-Change), Interaction Contract, Inherited-debt note (do NOT change the 10px marginBottom / 6px 0 padding on styles.dashboardLink this phase).
    - .planning/phases/21-dashboard-button-reposition/21-CONTEXT.md — locked decisions D-01 through D-06 and the Claude's-Discretion note on the divider.
  </read_first>
  <action>
    Per D-03/D-01/D-02: insert the `📊 View Dashboard` button immediately after the closing tag of the `<div style={styles.header}>` block and immediately before the `{feedPct !== null && ...}` feed-health paragraph. Render it as `<button onClick={openDashboard} style={styles.dashboardLink}>📊 View Dashboard</button>` — reusing the existing `openDashboard` handler verbatim and the existing `styles.dashboardLink` style object (D-06). Render it unconditionally (always visible — not gated by `feedPct`, list state, or any disclosure). Label text must be exactly `📊 View Dashboard` (D-02). Do not introduce a new style key, do not inline the styles, do not change `styles.dashboardLink` values (the 10px marginBottom and 6px 0 padding are inherited debt, out of scope per UI-SPEC).

    Per D-05: fully delete the in-settings `<button onClick={openDashboard} style={styles.dashboardLink}>📊 View Dashboard</button>` element inside the `<details>` Settings body. Do NOT touch the `openDashboard()` function definition (~line 101) — it stays and is now called from the relocated header button. After deletion the Settings body retains only: the threshold slider (`settingRow`), the mode/API-key section (`modeRow`, API key `input`), the Save/Clear `buttonRow`, and the `hint` paragraph.

    Divider discretion (Claude's discretion per CONTEXT.md): the `<hr style={styles.divider} />` immediately after the old in-settings button only existed to separate that button from the `modeRow`. With the button gone it is redundant — remove the `<hr style={styles.divider} />` so the slider sits directly above the mode row. Because nothing else references `styles.divider`, also remove the now-orphaned `divider` key from the `styles` record to avoid a dangling style object. Confirm via grep that `divider` has zero remaining references before removing the style key. Leave `styles.dashboardLink` in place (still referenced by the relocated button — must NOT be orphaned, D-06).

    Make no other changes: no new files, no edits outside `src/popup/index.tsx`, no behavior change to the open-in-new-tab handler (preserves the existing `noreferrer` new-tab open).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `src/popup/index.tsx` renders a `📊 View Dashboard` button between the `styles.header` block closing tag and the `feedPct` paragraph (it appears textually before the `{feedPct !== null` conditional).
    - The relocated button uses `onClick={openDashboard}` and `style={styles.dashboardLink}` with label exactly `📊 View Dashboard`.
    - `grep -c "openDashboard"` over `src/popup/index.tsx` shows the function is defined once and referenced from exactly ONE call site (single onClick) — i.e. no duplicate button remains inside the `<details>` Settings body.
    - The `<details>` Settings body no longer contains a View Dashboard button; it contains only the threshold slider, mode/API-key section, Save/Clear buttons, and hint.
    - `styles.dashboardLink` is still present in the `styles` record and still referenced (not orphaned).
    - `styles.divider` has no remaining references in the file; if the `divider` style key was removed it leaves no dangling reference, and if any reference to it remained the key was NOT removed (no dangling either way).
    - The `openDashboard()` function definition is unchanged (still `window.open(chrome.runtime.getURL('dashboard/index.html'), '_blank', 'noreferrer')`).
    - `npm run type-check` exits 0 (no TypeScript errors).
    - `npm run build` (vite build) completes successfully.
    - Only `src/popup/index.tsx` appears in `git diff --name-only` for this change (ROADMAP SC#4).
  </acceptance_criteria>
  <done>
    The View Dashboard button is rendered in the header region (above feed-health and the account list) and removed from Settings; exactly one openDashboard call site exists; styles.dashboardLink is retained and referenced; styles.divider is not left dangling; type-check and build pass; only src/popup/index.tsx changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| popup → browser tab | `window.open(chrome.runtime.getURL('dashboard/index.html'), '_blank', 'noreferrer')` opens a trusted extension-internal page in a new tab |

This phase relocates an existing button that calls an existing handler. It introduces **no new input handling, no new permissions, no new network requests, and no new DOM-injection surface**. The button text and URL are static literals; the URL is resolved via `chrome.runtime.getURL` (extension-internal, not user-controlled). The relocated button preserves the existing `noreferrer` new-tab open.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | Reverse tabnabbing via new-tab open | accept | Existing `noreferrer` is preserved on the relocated `window.open` call; no regression. Target is extension-internal `chrome.runtime.getURL` page, not an external origin. |
| T-21-02 | Information Disclosure | Static label/URL rendering | accept | No user-controlled or stored data is rendered by the relocated button; label and URL are compile-time string literals. No new disclosure surface. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks in this phase; no dependency changes. No new packages introduced. |

No new HIGH-severity findings. No genuine HIGH threat exists for a static-literal button relocation in a stateless popup, so there is nothing to block on (ASVS L1).
</threat_model>

<verification>
- `npm run type-check` passes (tsc --noEmit, exit 0).
- `npm run build` (vite build) completes successfully.
- `git diff --name-only` lists only `src/popup/index.tsx`.
- Manual/loaded-extension sanity (optional, not gating): opening the popup shows the button above the feed-health line; opening Settings shows no dashboard button; clicking the header button opens the dashboard in a new tab.
</verification>

<success_criteria>
1. POPUP-04: The 📊 View Dashboard button renders in the header region (between the title header and the feed-health line), visible on popup open without opening Settings.
2. The relocated button opens dashboard/index.html in a new tab (unchanged `openDashboard` behavior, new position).
3. POPUP-05: The ⚙ Settings disclosure no longer contains a View Dashboard button — only threshold slider, mode/API-key section, Save/Clear, and hint remain. Button is moved, not duplicated (one openDashboard call site).
4. SC#4: Change is confined to `src/popup/index.tsx`; no other files modified.
5. `styles.dashboardLink` is retained and reused (not orphaned); `styles.divider` is not left dangling.
6. type-check and build pass.
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-button-reposition/21-01-SUMMARY.md` when done.
</output>
