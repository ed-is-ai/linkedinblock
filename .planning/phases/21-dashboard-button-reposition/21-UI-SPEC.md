---
phase: 21
slug: dashboard-button-reposition
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-06
---

# Phase 21 — UI Design Contract

> Visual and interaction contract for Phase 21: Dashboard Button Reposition.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — Preact 10 + inline style objects only |
| Icon library | none — emoji glyphs only (e.g. `📊`, `⚙`) |
| Font | `-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` |

Source: `src/popup/index.tsx` styles record (lines 265–387); Phase 4 decision (inline styles only, no CSS class selectors).

---

## Spacing Scale

### Conformant token scale (multiples of 4 only)

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding |
| sm | 8px | `header` marginBottom, `details` padding/margin, `buttonRow` gap |
| md | 16px | `container` padding |
| lg | 24px | Not used in popup at this scale |
| xl | 32px | Not used in popup at this scale |
| 2xl | 48px | Not used in popup at this scale |
| 3xl | 64px | Not used in popup at this scale |

### Inherited debt — out of scope

The following values exist in `styles.dashboardLink` in the current codebase and are **not multiples of 4**. They are carried forward unchanged because this phase is a relocation only — no restyling. Correcting these to the nearest conformant value (e.g. `8px` or `12px`) is deferred to a future style-pass phase.

| Property | Current value | Status |
|----------|---------------|--------|
| `marginBottom` on `styles.dashboardLink` | 10px | Inherited — do NOT change this phase |
| `padding` on `styles.dashboardLink` | `6px 0` | Inherited — do NOT change this phase |

These values are excluded from the conformant token scale above. They do not represent approved spacing tokens; they represent pre-existing technical debt that is out of scope for Phase 21.

Source: `src/popup/index.tsx` styles record (lines 265–387).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / default | 13px | 400 (regular) | 1.5 (inherited) |
| Label / meta | 12px | 400 regular | 1.5 |
| Small / hint | 11px | 400 regular | 1.5 |
| Heading (title) | 16px | 600 (semibold) | 1.2 |

Source: `src/popup/index.tsx` — `container.fontSize: 13`, `title.fontSize: 16 / fontWeight: 600`, `summary.fontSize: 12`, `hint.fontSize: 11`.

The relocated `📊 View Dashboard` button uses `fontSize: 12` (inherited from `styles.dashboardLink`). Do not change.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#ffffff` (browser default) | Popup background, container surface |
| Secondary (30%) | `#f3f4f6` | `dashboardLink` button fill, `clearBtn` fill, input background |
| Accent (10%) | `#0a66c2` | Badge background, `saveBtn` background, threshold value text, links |
| Destructive | not applicable | No destructive actions in this phase |

Accent reserved for: badge pill, Save button, threshold numeric display, hyperlinks. NOT used on the `dashboardLink` button (which is secondary grey by design — D-04 from CONTEXT.md).

Border / separator: `#d1d5db` (button borders, input border), `#e5e7eb` (dividers, `details` border-top).
Text primary: `#1a1a1a`. Text secondary / meta: `#6b7280`. Text muted: `#555`.

Source: `src/popup/index.tsx` styles record; D-04 from `21-CONTEXT.md` (reuse existing grey dashboardLink style, no new accent color).

---

## Component Inventory

### Relocated Component: Dashboard Button

This phase moves — does not create — one existing button element.

| Property | Specification |
|----------|---------------|
| Element | `<button onClick={openDashboard} style={styles.dashboardLink}>📊 View Dashboard</button>` |
| Style key | `styles.dashboardLink` (reused verbatim — do NOT rename or orphan) |
| Render position | Immediately after `<div style={styles.header}>…</div>` closing tag; immediately before the `feedPct` paragraph (or the `listContainer` div if `feedPct` is null) |
| Width | 100% (full-width, matching prior treatment) |
| Background | `#f3f4f6` |
| Text color | `#374151` |
| Border | `1px solid #d1d5db` |
| Border-radius | 4px |
| Font size | 12px |
| Padding | `6px 0` (inherited — see Inherited debt note above) |
| Text alignment | center |
| marginBottom | 10px (inherited — see Inherited debt note above) |
| Cursor | pointer |

### Deleted Element: In-Settings Dashboard Button

Remove exactly the `<button>` element at `src/popup/index.tsx:219-221`:

```
<button onClick={openDashboard} style={styles.dashboardLink}>
  📊 View Dashboard
</button>
```

The `openDashboard()` function (line ~101) stays — it is now referenced by the relocated button.

### Discretionary: Settings Divider (`<hr>`)

The `<hr style={styles.divider} />` at line ~223 separated the dashboard button from the mode/API-key row. After button removal, evaluate whether it is still visually needed. If Settings reads cleanly without it, remove it. If the mode row benefits from a separator, keep it. Planner decides based on resulting Settings layout.

Source: CONTEXT.md § "Claude's Discretion".

---

## Interaction Contract

| Interaction | Behaviour |
|-------------|-----------|
| Click "📊 View Dashboard" (header) | `window.open(chrome.runtime.getURL('dashboard/index.html'), '_blank', 'noreferrer')` — identical to existing handler |
| Hover state | Browser default cursor:pointer — no custom hover styling (consistent with existing buttons) |
| Focus state | Browser default outline — no change from existing treatment |
| Settings open/close | Unchanged — `<details>` toggle; no longer contains dashboard button |

No new state, no new handlers. Stateless popup pattern is preserved.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Relocated button label | `📊 View Dashboard` (unchanged — D-02 from CONTEXT.md) |
| Empty state (no accounts) | `No flagged accounts yet — scroll LinkedIn to start detecting.` (existing, unchanged) |
| Feed health line | `{feedPct} of posts flagged (7d)` (existing, unchanged) |
| Error state | No error state in this phase — relocation only, no new async operations |
| Destructive confirmation | Not applicable — no destructive actions in this phase |

Source: CONTEXT.md D-02; `src/popup/index.tsx` line ~150 and ~145.

---

## Layout Structure (Post-Change)

```
<div style={styles.container}>                   ← 280px wide, 16px padding
  <div style={styles.header}>                    ← flex row, space-between
    <h2 style={styles.title}>LinkedIn Blocker</h2>
    {badge if pending > 0}
  </div>

  <button style={styles.dashboardLink}>          ← RELOCATED HERE (new)
    📊 View Dashboard
  </button>

  {feedPct paragraph}                            ← feedHealth style, unchanged
  <div style={styles.listContainer}>             ← account list, unchanged
    …
  </div>
  <BatchBlockBar />                              ← unchanged
  <details style={styles.details}>              ← Settings, threshold + API key only
    …threshold slider…
    …mode indicator…
    …API key input…
    …Save / Clear buttons…
    …hint paragraph…
    {NO dashboard button here — removed}
  </details>
</div>
```

---

## Scope Boundary

- Only `src/popup/index.tsx` is modified.
- No new component files.
- No changes to `AccountRow.tsx`, `BatchBlockBar.tsx`, service worker, content script, dashboard, or manifest.
- `styles.dashboardLink` style object is retained and reused — not inlined, not renamed, not deleted.

Source: CONTEXT.md § "Phase Boundary"; ROADMAP.md Phase 21 SC#4.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No external component registry is used. All UI is hand-authored inline styles in Preact.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
