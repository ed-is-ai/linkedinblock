---
phase: 02-detection-engine
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/content/detector/heuristic.ts
  - src/content/detector/heuristic.test.ts
  - src/content/exclusions.ts
  - src/content/exclusions.test.ts
  - src/content/detector/tombstone.ts
  - src/content/detector/tombstone.test.ts
  - src/content/detector/comment-expand.ts
autonomous: true
requirements: [DETECT-02, DETECT-03, DETECT-05, DETECT-06, DETECT-07, CONFIG-02, FEED-01]

must_haves:
  truths:
    - "HeuristicDetector implements the existing Detector interface from src/shared/types.ts without changing the interface signature (CONFIG-02, D-13)"
    - "Calling detector.detect(postData) returns DetectionResult with score (0-100 int), signals[], signalBreakdown, confidence, engineUsed='heuristic'"
    - "checkExclusions returns excluded=true for sponsored posts (D-12.1, DETECT-02), company-page authors (D-12.2, DETECT-03), and non-English text (D-12.3, DETECT-04)"
    - "checkExclusions returns excluded=false + openToWork=true when the OPEN_TO_WORK_MARKER is present (D-12.4)"
    - "injectTombstone creates a sibling div before postNode using textContent (not innerHTML) per D-08 and D-09 - text format 'Post by [author] hidden ([score]/100)'"
    - "Clicking the tombstone removes .llb-hidden from postNode and removes the tombstone element"
    - "expandComments is a stub returning [] when COMMENT_EXPAND_BUTTON is absent; clicks the button at most once per post; capped at 10 expansions per page load per RESEARCH Open Question 2"
    - "Detector signal interface allows Phase 3 to add DETECT-06 profile signals without changing detector.detect() callers (D-03)"
  artifacts:
    - path: "src/content/detector/heuristic.ts"
      provides: "HeuristicDetector class implementing Detector"
      contains: "implements Detector"
    - path: "src/content/exclusions.ts"
      provides: "checkExclusions(postData, postNode) returns ExclusionResult"
      exports: ["checkExclusions", "ExclusionResult"]
    - path: "src/content/detector/tombstone.ts"
      provides: "injectTombstone(postNode, authorName, score) DOM utility"
      exports: ["injectTombstone"]
    - path: "src/content/detector/comment-expand.ts"
      provides: "expandComments(postNode) returns Promise<string[]>"
      exports: ["expandComments"]
  key_links:
    - from: "src/content/detector/heuristic.ts"
      to: "src/content/detector/signals/*.ts"
      via: "import { checkListicle, checkBuzzwords, checkEmDash, checkCta, checkGenericComments }"
      pattern: "from ['\\\"]\\./signals/"
    - from: "src/content/exclusions.ts"
      to: "src/content/selectors.ts"
      via: "import { SPONSORED_MARKER, COMPANY_PAGE_MARKER, OPEN_TO_WORK_MARKER } from './selectors'"
      pattern: "from ['\\\"]\\./selectors['\\\"]"
    - from: "src/content/detector/tombstone.ts"
      to: "DOM"
      via: "document.createElement + parentNode.insertBefore"
      pattern: "insertBefore"
---

<objective>
Compose the pure signal functions from Plan 02 into the HeuristicDetector class, write the hard-exclusion guard, the tombstone DOM utility, and the comment-expansion helper. After this plan, every Phase 2 piece exists EXCEPT the final content/index.ts wiring and the background badge handler - those belong to Plan 04. The Detector interface signature (from Plan 01 / shared/types.ts) is unchanged, satisfying CONFIG-02 and D-13.

Purpose: Orchestration without wiring. HeuristicDetector calls the pure signal functions and applies D-05 weights including the "listicle + CTA opener combo = 25" composite signal, and gates the engagement signal behind a content-score > 20 threshold (per RESEARCH.md diagram, comments only expand when content signals warrant it - RESEARCH Open Question 2 caps expansions at 10 per page load).

Output:
- src/content/detector/heuristic.ts - HeuristicDetector class + unit tests with stub fetchComments (no real LinkedIn)
- src/content/exclusions.ts - sponsored / company / non-English / OtW pre-detection guard
- src/content/detector/tombstone.ts - sibling-div injection with click-to-reveal
- src/content/detector/comment-expand.ts - page-scoped expansion cap; safe-when-absent fallback
- DETECT-06 forward compatibility: a comment block in heuristic.ts marks the extension point where Phase 3 profile signals will plug in without changing detect()
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-detection-engine/02-CONTEXT.md
@.planning/phases/02-detection-engine/02-RESEARCH.md
@.planning/phases/02-detection-engine/02-PATTERNS.md
@.planning/phases/02-detection-engine/02-01-SUMMARY.md
@.planning/phases/02-detection-engine/02-02-SUMMARY.md
@src/shared/types.ts
@src/content/selectors.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HeuristicDetector class composes signals into a Detector implementation</name>
  <files>src/content/detector/heuristic.ts, src/content/detector/heuristic.test.ts</files>
  <read_first>
    - src/shared/types.ts (after Plan 01 - Detector interface signature; DetectionResult shape including signalBreakdown)
    - src/content/detector/signals/listicle.ts, buzzwords.ts, em-dash.ts, cta.ts, comments.ts (after Plan 02 - pure signal function signatures and weight returns)
    - .planning/phases/02-detection-engine/02-CONTEXT.md D-05 (weight table: listicle+cta combo=25, buzzword=15, em-dash=10, cta-close=10, generic-comments=15), D-04 (thresholds: auto-hide 60, flag-only 35), D-13 (interface locked)
    - .planning/phases/02-detection-engine/02-RESEARCH.md System Architecture Diagram (lines 158-203 - signal call order, content-score > 20 gate before comment expansion)
    - .planning/phases/02-detection-engine/02-PATTERNS.md src/content/detector/heuristic.ts section (concrete skeleton, lines 316-340)
  </read_first>
  <behavior>
    - new HeuristicDetector().name === 'heuristic'
    - detect({postText: "I went to the shops today and bought milk."}) returns score 0, signals=[], signalBreakdown={}, confidence='low', engineUsed='heuristic'
    - detect with a known listicle + CTA opener combo populates signalBreakdown with key 'listicle-cta' value 25
    - detect with heavy buzzword text populates signalBreakdown.buzzword === 15
    - detect with 50-word text containing 3 em-dashes populates signalBreakdown['em-dash'] >= 5
    - For a post whose pre-comment content score is <= 20, the comments path is NOT invoked (no 'generic-comments' key)
    - detect returns score = Math.min(sum, 100) - never exceeds 100
    - confidence: score >= 60 -> 'high'; 35-59 -> 'medium'; < 35 -> 'low'
    - signals[] is exactly Object.keys(signalBreakdown)
  </behavior>
  <action>
    Create `src/content/detector/heuristic.ts` exporting `class HeuristicDetector implements Detector` with:
    - `readonly name = 'heuristic'`
    - Constructor signature `constructor(options?: { fetchComments?: (post: PostData) => Promise<string[]> })`. fetchComments is the comment source so heuristic.test.ts can pass a Promise-returning stub and the production wiring in Plan 04 can pass a function that calls `expandComments(postNode)`. Keeping DOM access OUT of HeuristicDetector preserves test purity.
    - `async detect(post: PostData): Promise<DetectionResult>` implementing the pipeline from 02-RESEARCH.md System Architecture Diagram:
      1. Initialise `const breakdown: Record<string, number> = {}` and `let score = 0`.
      2. Compute listicleScore (checkListicle) and ctaScore (checkCta) on post.postText.
      3. Apply the "listicle + CTA opener combo = 25" rule from D-05: if BOTH listicleScore > 0 AND ctaScore > 0 set `breakdown['listicle-cta'] = 25` and add 25 to score. Else if only listicleScore > 0 set `breakdown['listicle-cta'] = 12`. Else if only ctaScore > 0 set `breakdown['listicle-cta'] = 8`. (Match the skeleton in 02-PATTERNS.md line 668.)
      4. buzzScore = checkBuzzwords(post.postText). If > 0 set `breakdown.buzzword = buzzScore` and add to score.
      5. emDashScore = checkEmDash(post.postText). If > 0 set `breakdown['em-dash'] = emDashScore` and add to score.
      6. NOTE: The "Generic CTA close phrase" weight from D-05 is already absorbed in step 3 because checkCta returns the combined opener+closer weight. Do NOT double-count by calling checkCta twice the way the RESEARCH.md Code Example skeleton appears to (that excerpt has a documented duplicate). Add a JSDoc note explaining this decision and citing D-05.
      7. Engagement gate (D-02 + RESEARCH diagram): if `score > 20` AND `this.options.fetchComments` is defined, await `this.options.fetchComments(post)` and pass the returned string[] to `checkGenericComments`. If > 0 set `breakdown['generic-comments'] = commentScore` and add to score.
      8. `const finalScore = Math.min(score, 100);`
      9. Return `{ score: finalScore, signals: Object.keys(breakdown), signalBreakdown: { ...breakdown }, confidence: finalScore >= 60 ? 'high' : finalScore >= 35 ? 'medium' : 'low', engineUsed: 'heuristic' }`.
    - Include a doc-comment block above detect() with this exact wording: `DETECT-06 extension point (Phase 3): profile-signal scores plug in here as additional breakdown entries before the Math.min cap. Do NOT change detect()'s signature.` This is the documented "interface for Phase 3" required by DETECT-06 + D-03.

    Create `src/content/detector/heuristic.test.ts` covering the bullet list in <behavior>. Use a stub fetchComments that returns at least two long strings (> 20 chars) matching the GENERIC_PHRASES set used by checkGenericComments and assert `breakdown['generic-comments']` is set when content score crosses 20.
  </action>
  <verify>
    <automated>npx vitest run src/content/detector/heuristic.test.ts --reporter=basic && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/detector/heuristic.ts` exports `class HeuristicDetector` and `implements Detector` appears in the class declaration
    - Constructor accepts an optional `{ fetchComments?: (post: PostData) => Promise<string[]> }`
    - `grep -n "score >= 60 \? 'high'" src/content/detector/heuristic.ts` matches
    - `grep -n "DETECT-06" src/content/detector/heuristic.ts` matches (Phase 3 extension point comment present)
    - `grep -n "Math.min" src/content/detector/heuristic.ts` matches
    - File does NOT contain the strings `document.`, `chrome.`, or any LinkedIn selector literal (orchestration only)
    - heuristic.test.ts has at least 6 assertions covering <behavior> and exits 0 under `npx vitest run`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>HeuristicDetector composes Plan 02 signals into a Detector-conformant class; tested without a browser; DETECT-06 extension point documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hard-exclusion guard (sponsored / company / non-English / Open-to-Work)</name>
  <files>src/content/exclusions.ts, src/content/exclusions.test.ts</files>
  <read_first>
    - src/content/selectors.ts (after Plan 01 - SPONSORED_MARKER, COMPANY_PAGE_MARKER, OPEN_TO_WORK_MARKER)
    - src/content/detector/language.ts (after Plan 02 - isNonEnglish signature)
    - src/shared/types.ts (PostData)
    - .planning/phases/02-detection-engine/02-CONTEXT.md D-12 (the four hard-exclusion clauses in priority order)
    - .planning/phases/02-detection-engine/02-PATTERNS.md src/content/exclusions.ts section (concrete shape, lines 521-559)
    - .planning/phases/02-detection-engine/02-RESEARCH.md Pattern 2: Hard Exclusion Check
  </read_first>
  <action>
    Create `src/content/exclusions.ts` with:
    - `export interface ExclusionResult { excluded: boolean; reason?: 'sponsored' | 'company-page' | 'non-english'; openToWork?: boolean; }`
    - `export function checkExclusions(postData: PostData, postNode: Element): ExclusionResult` implementing D-12 in this exact priority order:
      1. SPONSORED_MARKER query (truthy) -> return `{ excluded: true, reason: 'sponsored' }` (DETECT-02)
      2. `postData.authorProfileUrl.includes(COMPANY_PAGE_MARKER)` -> return `{ excluded: true, reason: 'company-page' }` (DETECT-03)
      3. `isNonEnglish(postNode, postData.postText)` -> return `{ excluded: true, reason: 'non-english' }` (DETECT-04)
      4. Open to Work: `const openToWork = !!postNode.querySelector(OPEN_TO_WORK_MARKER);` return `{ excluded: false, openToWork }`.
    - Imports come exclusively from `./selectors`, `./detector/language`, and `../shared/types`. Do NOT inline any selector string (CLAUDE.md constraint #1, INFRA-04).
    - Add a doc comment mapping each branch to its requirement ID: DETECT-02 (sponsored), DETECT-03 (company-page), DETECT-04 (non-English), D-12.4 (OtW threshold raise - the +20 adjustment itself is applied by the caller in Plan 04, not here).

    Create `src/content/exclusions.test.ts` with assertions:
    - Sponsored: a postNode whose querySelector for SPONSORED_MARKER returns a truthy element -> result.excluded === true && reason === 'sponsored'
    - Company: authorProfileUrl 'https://linkedin.com/company/google/' -> reason === 'company-page'
    - Non-English: stub isNonEnglish to return true (or use a postNode mock + CJK text) -> reason === 'non-english'
    - OtW present: result.excluded === false && result.openToWork === true
    - Clean post: result.excluded === false && result.openToWork === false
    - Priority: sponsored takes precedence over OtW (a sponsored OtW post still returns excluded:'sponsored').
  </action>
  <verify>
    <automated>npx vitest run src/content/exclusions.test.ts --reporter=basic && npx tsc --noEmit && node -e "const s = require('fs').readFileSync('src/content/exclusions.ts','utf8'); const needs = ['SPONSORED_MARKER','COMPANY_PAGE_MARKER','OPEN_TO_WORK_MARKER','isNonEnglish','interface ExclusionResult']; const miss = needs.filter(n => !s.includes(n)); if (miss.length) { console.error('missing:', miss); process.exit(1); } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/exclusions.ts` exports `checkExclusions` and `interface ExclusionResult`
    - All three selector constants (SPONSORED_MARKER, COMPANY_PAGE_MARKER, OPEN_TO_WORK_MARKER) are imported from `./selectors` and never inlined
    - Branch order matches D-12 exactly: sponsored -> company -> non-English -> OtW (verified by reading the file top-to-bottom)
    - exclusions.test.ts has at least 6 assertions and exits 0 under `npx vitest run`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Pre-detection exclusion guard ships covering DETECT-02/03/04 and the OtW threshold modifier surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tombstone DOM utility + safe comment-expansion helper</name>
  <files>src/content/detector/tombstone.ts, src/content/detector/tombstone.test.ts, src/content/detector/comment-expand.ts</files>
  <read_first>
    - .planning/phases/02-detection-engine/02-CONTEXT.md D-07, D-08, D-09 (tombstone format and sibling injection rule)
    - .planning/phases/02-detection-engine/02-RESEARCH.md Pattern 5: Tombstone Injection (lines 433-468) and Pitfall 4 (sibling injection, NOT inside post)
    - .planning/phases/02-detection-engine/02-RESEARCH.md Open Question 2 (cap comment expansion at 10 per page load)
    - .planning/phases/02-detection-engine/02-PATTERNS.md src/content/detector/tombstone.ts section (lines 483-516)
    - src/content/selectors.ts (after Plan 01 - COMMENT_EXPAND_BUTTON, COMMENT_TEXT)
    - CLAUDE.md constraint #2 (no element.remove() on LinkedIn-owned nodes; tombstone.remove() is allowed because the tombstone is OUR injected node)
  </read_first>
  <action>
    Create `src/content/detector/tombstone.ts` exporting `injectTombstone(postNode: Element, authorName: string, score: number): void`:
    - `const tombstone = document.createElement('div')`
    - `tombstone.className = 'llb-tombstone'`
    - `tombstone.setAttribute('role', 'button')`
    - `tombstone.setAttribute('aria-label', \`Reveal post by ${authorName}\`)`
    - `tombstone.textContent = \`Post by ${authorName} hidden (${score}/100)\`` - use textContent ONLY (D-09 tombstone XSS mitigation; never innerHTML). authorName is already DOM-derived text, but we still pass it through textContent to be defensive.
    - Click handler: removes `'llb-hidden'` from `postNode.classList` and calls `tombstone.remove()`. The tombstone is our own injected node; removing it is allowed (CLAUDE.md constraint #2 forbids removing LinkedIn-owned nodes, which postNode is - and postNode is NOT removed; only the class is toggled).
    - Inject as sibling BEFORE postNode: `postNode.parentNode?.insertBefore(tombstone, postNode)` per D-08 and Pitfall 4 (NEVER inject inside postNode - React will reconcile it away).
    - Do NOT inject any CSS here. The `<style>` block lives in `content/index.ts` (Plan 04) and already covers both `.llb-hidden` and `.llb-tombstone` rules.

    Create `src/content/detector/tombstone.test.ts` using a jsdom-style document setup:
    - Build a parent div with a postNode child
    - Call injectTombstone(postNode, 'Jane Smith', 74)
    - Assert: parent's children are now [tombstone, postNode] in that order
    - Assert: tombstone.textContent === 'Post by Jane Smith hidden (74/100)'
    - Assert: tombstone.getAttribute('role') === 'button'
    - Simulate click on tombstone -> postNode.classList.contains('llb-hidden') === false AND tombstone.parentNode === null

    Create `src/content/detector/comment-expand.ts` exporting `async function expandComments(postNode: Element): Promise<string[]>` with these rules:
    - Module-scope counter `let pageExpansionCount = 0` and `const MAX_EXPANSIONS_PER_PAGE = 10` (per RESEARCH Open Question 2).
    - If `pageExpansionCount >= MAX_EXPANSIONS_PER_PAGE` return `[]` immediately.
    - Find the expand button via `postNode.querySelector(COMMENT_EXPAND_BUTTON) as HTMLElement | null`. If null, return `[]` (Pitfall 2 silent-degrade is acceptable).
    - Click the button via `button.click()`. Increment pageExpansionCount.
    - Await a short delay using `await new Promise(r => setTimeout(r, 800))` to allow LinkedIn to render the comment section.
    - Collect comment text with `Array.from(postNode.querySelectorAll(COMMENT_TEXT)).map(el => (el as HTMLElement).innerText.trim()).filter(Boolean)` - use innerText not innerHTML; filter empties.
    - Cap returned array at 20 entries (matches the eligible cap inside checkGenericComments and bounds O(n^2)).
    - Wrap the entire body in `try { ... } catch { return []; }` - clicking on a stale or detached node must not throw to the caller.
    - Export `resetExpansionBudget()` to reset pageExpansionCount; this is called from Plan 04's SPA navigation hook so a new feed view gets a fresh budget. Document this in a JSDoc comment.
    - Add JSDoc citing D-02 (read-only action; RESEARCH Open Question 2 cap).
  </action>
  <verify>
    <automated>npx vitest run src/content/detector/tombstone.test.ts --reporter=basic && npx tsc --noEmit && node -e "const s = require('fs').readFileSync('src/content/detector/tombstone.ts','utf8'); if (!/textContent/.test(s) || /innerHTML/.test(s)) { console.error('tombstone DOM safety drift'); process.exit(1); } if (!/insertBefore/.test(s)) { console.error('tombstone not sibling-injected'); process.exit(1); } const c = require('fs').readFileSync('src/content/detector/comment-expand.ts','utf8'); if (!/MAX_EXPANSIONS_PER_PAGE/.test(c)) { console.error('expansion cap missing'); process.exit(1); } if (!/COMMENT_EXPAND_BUTTON/.test(c) || !/COMMENT_TEXT/.test(c)) { console.error('selector import missing'); process.exit(1); } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - tombstone.ts uses `textContent =` and never `innerHTML`
    - tombstone.ts uses `parentNode.insertBefore(tombstone, postNode)` (sibling injection)
    - tombstone click handler removes `'llb-hidden'` from postNode.classList AND calls `tombstone.remove()` (assertable by jsdom test)
    - tombstone.test.ts has at least 4 assertions and exits 0 under `npx vitest run`
    - comment-expand.ts declares `MAX_EXPANSIONS_PER_PAGE = 10`
    - comment-expand.ts wraps its body in try/catch returning `[]` on any throw
    - comment-expand.ts exports `resetExpansionBudget`
    - `grep -rn "innerHTML" src/content/detector/` returns no matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Tombstone injection ships with unit tests; comment expansion respects the 10-per-page cap and silently no-ops when the selector is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LinkedIn DOM -> tombstone textContent | authorName flows from DOM text into our injected node |
| LinkedIn DOM -> comment-expand click | Programmatic click on LinkedIn-owned button (read-only per D-02) |
| Pure detector -> caller-supplied fetchComments | Comments string array crosses from DOM tier into pure scorer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering (XSS) | injectTombstone authorName interpolation | mitigate | Use `tombstone.textContent =` exclusively; never `innerHTML`. Verified by automated grep in Task 3 verify (no `innerHTML` under src/content/detector/) |
| T-02-10 | Tampering | Click on LinkedIn comment-expand button | accept | Read-only action per D-02; capped at 10 expansions/page (RESEARCH Open Question 2) limits server request volume; try/catch around the click swallows extension-context-invalidated errors |
| T-02-11 | Repudiation/ToS | Programmatic clicks on LinkedIn UI | mitigate | Only the comment-expand button (read-only) is clicked. Block/follow/like buttons are NEVER clicked - those use the deep-link approach in Phase 5 per CLAUDE.md constraint #3 |
| T-02-12 | Tampering | Tombstone reconciled away by React | mitigate | Sibling injection (insertBefore parent), not nested inside postNode (Pitfall 4). The post's parent is a List container in LinkedIn's tree; siblings injected at this granularity have survived in Phase 1 (.llb-hidden class persists across reconciliation cycles, per STATE.md research flags) |
| T-02-13 | Information Disclosure | DetectionResult.signalBreakdown reaches storage | mitigate | signalBreakdown contains numbers only (signal name -> score). No postText, comment text, or PII ever enters DetectionResult. Verified by FlaggedAccountStub shape from Plan 01 - no field accepts post body content |
</threat_model>

<verification>
- `npx vitest run` exits 0 across heuristic / exclusions / tombstone tests
- `npx tsc --noEmit` exits 0
- `grep -rn "innerHTML" src/content/` returns no matches
- `grep -rn "data-test-id\|data-urn\|aria-label" src/content/detector/` shows matches ONLY inside JSDoc comments (selector strings live in selectors.ts)
- HeuristicDetector contains the literal `DETECT-06` extension-point comment
</verification>

<success_criteria>
- DETECT-02, DETECT-03, DETECT-04 enforced by checkExclusions in the priority order required by D-12
- DETECT-05 satisfied: DetectionResult.signalBreakdown carries per-signal scores
- DETECT-06 has a documented extension point in HeuristicDetector (interface ready; implementation deferred to Phase 3 per D-03)
- DETECT-07 wired: HeuristicDetector calls fetchComments behind a content-score > 20 gate; checkGenericComments scores returned strings
- CONFIG-02 satisfied: HeuristicDetector implements Detector with the unchanged `detect(post: PostData): Promise<DetectionResult>` signature
- FEED-01 partially satisfied: tombstone DOM helper exists and is verified to use sibling injection + class toggle (no element.remove on postNode). Final wiring of `.llb-hidden` to the post happens in Plan 04
- Plan 04 can wire content/index.ts purely by calling: checkExclusions -> detector.detect (constructed with a fetchComments lambda that calls expandComments) -> injectTombstone -> chrome.runtime.sendMessage
</success_criteria>

<output>
Create `.planning/phases/02-detection-engine/02-03-SUMMARY.md` when done summarising:
- HeuristicDetector composes Plan 02 signals; DETECT-06 extension point documented for Phase 3
- checkExclusions guards DETECT-02/03/04 in D-12 priority order; OtW returns metadata for the caller to apply the +20 threshold
- Tombstone uses textContent + sibling injection per D-08/D-09 and Pitfall 4
- expandComments capped at 10/page-load per RESEARCH Open Question 2; resetExpansionBudget exported for SPA-navigation hook
- All four new modules unit-tested without a browser; selector literals continue to live exclusively in selectors.ts
</output>
