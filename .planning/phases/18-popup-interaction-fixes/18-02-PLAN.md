---
phase: 18-popup-interaction-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/popup/index.tsx
  - src/popup/AccountRow.tsx
autonomous: true
requirements: [POPUP-01, POPUP-02]
must_haves:
  truths:
    - "Clicking an account name opens that profile's LinkedIn URL in a new tab and does NOT expand the row"
    - "Clicking the Block button writes status:'blocked' to chrome.storage.local and does not navigate anywhere"
  artifacts:
    - path: "src/popup/AccountRow.tsx"
      provides: "account name anchor with stopPropagation"
      contains: "stopPropagation"
    - path: "src/popup/index.tsx"
      provides: "handleBlock writing blocked status with no window.open"
      contains: "status: 'blocked'"
  key_links:
    - from: "src/popup/AccountRow.tsx name anchor onClick"
      to: "parent summaryArea onToggle"
      via: "e.stopPropagation() prevents toggle firing"
      pattern: "onClick=\\{\\(e\\) => e\\.stopPropagation"
    - from: "src/popup/index.tsx handleBlock"
      to: "chrome.storage.local"
      via: "set flaggedAccounts with status blocked, no window.open"
      pattern: "status: 'blocked'"
---

<objective>
Wire two popup one-liner fixes:
- POPUP-01 (D-04): clicking the account name should open the LinkedIn profile in a new tab without also toggling row expansion. The `<a>` already has correct `href`/`target="_blank"`/`rel="noreferrer"` but sits inside a `<div onClick={onToggle}>`, so clicks bubble and expand the row. Add `e.stopPropagation()` to the anchor's `onClick`.
- POPUP-02 (D-05): the Block button should write `status: 'blocked'` to storage only. Remove the trailing `window.open(url, ...)` call in `handleBlock` so the popup performs no navigation.

Purpose: Block becomes a local-only action (the "Report on LinkedIn" link is deferred per CONTEXT.md), and the profile link stops fighting the row-expand interaction.
Output: Updated `src/popup/AccountRow.tsx` (anchor stopPropagation) and `src/popup/index.tsx` (handleBlock window.open removal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-popup-interaction-fixes/18-CONTEXT.md
@.planning/phases/18-popup-interaction-fixes/18-RESEARCH.md
@.planning/phases/18-popup-interaction-fixes/18-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: POPUP-01 — stopPropagation on account name anchor</name>
  <files>src/popup/AccountRow.tsx</files>
  <read_first>
    - src/popup/AccountRow.tsx — the `summaryArea` wrapper `<div style={rowStyles.summaryArea} onClick={onToggle}>` (line 139) and the account name `<a>` element (lines 141–148). The anchor already has `href={account.authorProfileUrl}`, `target="_blank"`, `rel="noreferrer"`, `style={rowStyles.nameLink}`. The action buttons at lines 196–197 already use the `e.stopPropagation()` pattern — replicate it.
  </read_first>
  <action>
    Add `onClick={(e) => e.stopPropagation()}` to the account name `<a>` element (lines 141–148), keeping all existing attributes (`href`, `target`, `rel`, `style`) unchanged. This prevents the click from bubbling to the parent `summaryArea` div's `onToggle`, so clicking the name opens the profile (native anchor behavior) without expanding the row. Per RESEARCH Pitfall 4, no keyboard-specific handling is needed — the native `<a>` Enter activation does not trigger the parent onClick.

    Do not change `href`, `target`, or `rel` — they are already correct (D-04). Do not add `preventDefault` — that would block navigation.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - The account name `<a>` element has `onClick={(e) => e.stopPropagation()}`
    - The anchor still has `href={account.authorProfileUrl}`, `target="_blank"`, and `rel="noreferrer"`
    - No `preventDefault` is added to the anchor
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Clicking the account name opens the profile in a new tab and does not toggle row expansion.</done>
</task>

<task type="auto">
  <name>Task 2: POPUP-02 — remove window.open from handleBlock</name>
  <files>src/popup/index.tsx</files>
  <read_first>
    - src/popup/index.tsx — `handleBlock` (lines 57–69). It already reads `flaggedAccounts`, sets `flaggedAccounts[account.authorId] = { ...existing, status: 'blocked' as const }`, and writes back via `chrome.storage.local.set`. The two lines to delete are the `const url = account.authorProfileUrl || ...` (line 67) and `window.open(url, '_blank', 'noreferrer')` (line 68), plus the `// Open profile page...` comment on line 66.
  </read_first>
  <action>
    In `handleBlock`, delete the trailing navigation lines: the comment `// Open profile page so user can complete the block in LinkedIn` (line 66), the `const url = account.authorProfileUrl || \`https://www.linkedin.com/in/${account.authorId}/\`;` declaration (line 67), and the `window.open(url, '_blank', 'noreferrer');` call (line 68). Keep the storage read/merge/write logic (lines 59–65) exactly as is — `status: 'blocked' as const` write is correct per D-05/D-06.

    Leave the unrelated `openDashboard` function's `window.open` (line 92) untouched — only the one inside `handleBlock` is removed. After this change, `handleBlock` does a storage write and nothing else; the existing `onChanged` listener in the content script picks up the blocked status (D-06).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `handleBlock` contains `status: 'blocked' as const` and the `chrome.storage.local.set({ flaggedAccounts })` write
    - `handleBlock` body contains no `window.open` call
    - The `openDashboard` function still contains its `window.open(chrome.runtime.getURL(` call (untouched)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Block writes blocked status to storage and performs no navigation; dashboard link still opens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Popup → chrome.storage.local | handleBlock writes account status |
| Popup → new browser tab | account name anchor opens external LinkedIn URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | account name anchor target | accept | `rel="noreferrer"` already set on anchor; opens user's own LinkedIn profile URL from stored data |
| T-18-05 | Elevation of privilege | handleBlock storage write | mitigate | Write scoped to existing account by authorId key; no new keys, no schema change (D-06) |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `npx vite build` completes without error.
- Manual sanity (optional): in the popup, click an account name → opens profile in new tab, row does NOT expand; click Block → account disappears from pending list (moves to blocked, surfaced by Plan 03) with no new tab opening.
</verification>

<success_criteria>
- Account name click opens profile without expanding row (POPUP-01 / D-04).
- Block writes blocked status only, no navigation (POPUP-02 / D-05).
- Dashboard link behavior unchanged.
- TypeScript compiles clean.
</success_criteria>

<output>
Create `.planning/phases/18-popup-interaction-fixes/18-02-SUMMARY.md` when done.
</output>
