---
phase: 18-popup-interaction-fixes
plan: 03
type: execute
wave: 2
depends_on: ["18-02"]
files_modified:
  - src/popup/index.tsx
  - src/popup/AccountRow.tsx
autonomous: true
requirements: [POPUP-03]
must_haves:
  truths:
    - "Blocked accounts appear in a separate collapsible section below the pending list, headed 'Blocked (N) ▸/▾'"
    - "The Blocked section is collapsed by default and toggles open/closed on header click"
    - "A blocked AccountRow shows grey name text, a 'Blocked' chip, and renders no Block or Dismiss buttons"
    - "When blocked count is 0 the section is not rendered at all"
    - "Blocking an account moves it live from the pending list into the Blocked section via the existing onChanged listener"
  artifacts:
    - path: "src/popup/AccountRow.tsx"
      provides: "isBlocked prop variant: grey name, Blocked chip, no action buttons"
      contains: "isBlocked"
    - path: "src/popup/index.tsx"
      provides: "blocked filter + collapsible Blocked section"
      contains: "blockedExpanded"
  key_links:
    - from: "src/popup/index.tsx App"
      to: "AccountRow isBlocked prop"
      via: "blocked.map renders rows with isBlocked={true}"
      pattern: "isBlocked=\\{true\\}"
    - from: "src/popup/index.tsx blocked filter"
      to: "accounts state"
      via: "accounts.filter(a => a.status === 'blocked')"
      pattern: "status === 'blocked'"
    - from: "src/popup/AccountRow.tsx actionRow"
      to: "isBlocked guard"
      via: "actionRow rendered only when !isBlocked"
      pattern: "!isBlocked"
---

<objective>
Implement POPUP-03 (D-07, D-08): surface blocked accounts in a separate collapsible "Blocked (N)" section beneath the pending list, and give `AccountRow` an `isBlocked` visual variant (grey name, "Blocked" chip, no action buttons). Currently accounts with `status === 'blocked'` are filtered out of `pending` and never rendered — after Plan 02 made Block a local-only write, blocked accounts vanish entirely. This plan makes them visible and read-only.

Purpose: Gives the user a record of who they have blocked without re-cluttering the actionable pending list, and prevents action buttons on already-blocked rows (unblock is deferred to Phase 19).
Output: Updated `src/popup/index.tsx` (blocked filter, `blockedExpanded` toggle, collapsible section, `blockedSectionHeader` style) and `src/popup/AccountRow.tsx` (`isBlocked` prop, grey name, blocked chip, conditional actionRow, `blockedChip` style).

Note: This plan depends on Plan 02 (18-02) because both edit `src/popup/index.tsx` and `src/popup/AccountRow.tsx`. Run after 18-02 completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-popup-interaction-fixes/18-CONTEXT.md
@.planning/phases/18-popup-interaction-fixes/18-RESEARCH.md
@.planning/phases/18-popup-interaction-fixes/18-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AccountRow isBlocked prop — grey name, Blocked chip, conditional action buttons</name>
  <files>src/popup/AccountRow.tsx</files>
  <read_first>
    - src/popup/AccountRow.tsx — `AccountRowProps` interface (lines 4–11), the `rowStyles` record (lines 13–125, note the existing greys: `nameLink` color #1a1a1a, `chip` background #f3f4f6 / color #374151, `moreChips` color #9ca3af), the destructured props (lines 127–130), the name `<a>` element (lines 141–148, which gains stopPropagation in Plan 02), and the `actionRow` div (lines 195–198) with Dismiss + Block buttons.
    - .planning/phases/18-popup-interaction-fixes/18-UI-SPEC.md — Component Inventory "AccountRow.tsx — isBlocked prop variant" table and "Blocked chip style" block (the exact style values).
  </read_first>
  <action>
    Add `isBlocked?: boolean;` to the `AccountRowProps` interface (after `posts?`). Add `isBlocked = false` to the destructured props in the function signature.

    Add a new `rowStyles.blockedChip` entry with these exact values (per UI-SPEC D-08): `background: '#f3f4f6'`, `color: '#9ca3af'`, `fontSize: 10`, `padding: '2px 6px'`, `borderRadius: 10`, `border: '1px solid #e5e7eb'`, typed `as JSX.CSSProperties`.

    On the account name `<a>` element, make the style conditional: `style={{ ...rowStyles.nameLink, ...(isBlocked ? { color: '#9ca3af' } : {}) }}` so blocked names render in muted grey #9ca3af (D-08) while preserving weight 600 and all other nameLink properties. Keep the Plan 02 `onClick={(e) => e.stopPropagation()}` intact.

    Add the "Blocked" chip: render `{isBlocked && <span style={rowStyles.blockedChip}>Blocked</span>}` adjacent to the name. Place it inside `topLine` (lines 140–152), after the `<a>` and before or after the `peakScore` span — keep it on the same top line as the name per UI-SPEC.

    Guard the `actionRow` div (lines 195–198) so it renders only when not blocked: wrap it as `{!isBlocked && ( <div style={rowStyles.actionRow}>...Dismiss...Block...</div> )}`. Per RESEARCH Pitfall 3, do NOT keep the buttons rendered with no-op handlers — conditionally render the entire actionRow so blocked rows have no Block/Dismiss buttons at all.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `AccountRowProps` contains `isBlocked?: boolean`
    - The function signature destructures `isBlocked = false`
    - `rowStyles` contains a `blockedChip` entry with `color: '#9ca3af'` and `border: '1px solid #e5e7eb'`
    - The name anchor style spreads `rowStyles.nameLink` and conditionally applies `color: '#9ca3af'` when isBlocked
    - The source contains `{isBlocked &&` rendering a span with text `Blocked`
    - The `actionRow` div is wrapped in `{!isBlocked && (` — no Block/Dismiss buttons render when isBlocked
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AccountRow with isBlocked={true} renders a grey name, a "Blocked" chip, and no action buttons; default (isBlocked=false) rows are visually unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: index.tsx — blocked filter, collapsible Blocked section, header style</name>
  <files>src/popup/index.tsx</files>
  <read_first>
    - src/popup/index.tsx — the `useState` hook imports (line 2), the existing state declarations (lines 8–15), the `pending` filter+sort (lines 113–115), the `listContainer` render block where `pending` is mapped to `AccountRow` (lines 130–152), the `styles` record (lines 219–333, note `summary` style at 258–264 and `details`/divider greys), and the existing `onChanged` listener that updates `accounts` (lines 44–55) — no new listener wiring needed (D-06).
    - .planning/phases/18-popup-interaction-fixes/18-UI-SPEC.md — "index.tsx — Blocked section (new)" table (default collapsed, ▾/▸ indicators, header text "Blocked (N) ▾/▸") and "Blocked section header style" block (exact values).
  </read_first>
  <action>
    Add state: `const [blockedExpanded, setBlockedExpanded] = useState(false);` (collapsed by default per D-07 / Claude's discretion in CONTEXT.md). `useState` is already imported.

    Add a `blocked` filter mirroring the existing `pending` filter: `const blocked = accounts.filter(a => a.status === 'blocked').sort((a, b) => b.peakScore - a.peakScore);` placed right after the `pending` declaration (line 115).

    Add a `styles.blockedSectionHeader` entry with these exact values (per UI-SPEC): `cursor: 'pointer'`, `fontSize: 11`, `color: '#6b7280'`, `padding: '6px 0'`, `borderTop: '1px solid #e5e7eb'`, `userSelect: 'none' as const`.

    In the render, after the `listContainer` div (closes line 152) and before the `<details>` settings block (line 154), insert the collapsible section, rendered only when `blocked.length > 0`:
      - A header `<div style={styles.blockedSectionHeader} onClick={() => setBlockedExpanded(prev => !prev)}>` containing `Blocked ({blocked.length}) {blockedExpanded ? '▾' : '▸'}`.
      - When `blockedExpanded` is true, map `blocked` to `<AccountRow>` passing `key={account.authorId}`, `account={account}`, `onBlock={() => {}}`, `onDismiss={() => {}}`, `isBlocked={true}`, and `posts={storedPosts.filter(p => p.authorId === account.authorId).slice(0, 3)}`. The no-op `onBlock`/`onDismiss` are safe here because Task 1 conditionally removes the action buttons (RESEARCH Pitfall 3), so they are never invoked. Pass `isExpanded`/`onToggle` consistently with the pending rows if expansion of blocked rows is desired (planner discretion: expansion may be omitted for blocked rows since they have no actions — if omitted, do not pass isExpanded/onToggle).

    Do not add a new `chrome.storage.onChanged` listener — the existing one (lines 44–55) already refreshes `accounts`, so a freshly-blocked account moves from pending into the Blocked section automatically (D-06).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - index.tsx contains `const [blockedExpanded, setBlockedExpanded] = useState(false)`
    - index.tsx contains `accounts.filter(a => a.status === 'blocked')`
    - `styles` contains a `blockedSectionHeader` entry with `fontSize: 11`, `color: '#6b7280'`, and `borderTop: '1px solid #e5e7eb'`
    - The render contains a `blocked.length > 0` guard around the section
    - The header renders `Blocked (` with `{blocked.length}` and the `{blockedExpanded ? '▾' : '▸'}` indicator
    - Blocked rows are rendered with `isBlocked={true}`
    - No second `chrome.storage.onChanged.addListener` is added
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A collapsed "Blocked (N) ▸" section appears below the pending list when blocked accounts exist; expanding it shows read-only blocked rows; blocking an account moves it there live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → popup | accounts (incl. blocked) read and rendered |
| Popup render → DOM | blocked authorName rendered as JSX text (Preact escapes by default) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering | blocked authorName render | mitigate | Rendered via JSX `{account.authorName}` — Preact escapes text nodes, no innerHTML |
| T-18-07 | Elevation of privilege | blocked row action buttons | mitigate | actionRow conditionally removed for isBlocked rows (no Block/Dismiss possible); unblock deferred to Phase 19 |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `npx vite build` completes without error.
- Manual sanity (optional): block an account in the popup → it leaves pending and appears under "Blocked (N) ▸"; click the header → section expands showing a grey-name row with a "Blocked" chip and no buttons; with zero blocked accounts the header is absent.
</verification>

<success_criteria>
- Blocked accounts render in a collapsible section, collapsed by default (D-07).
- Blocked rows are grey-named, chipped, and button-free (D-08).
- Section hidden when blocked count is 0.
- Live move from pending to blocked via existing onChanged (D-06).
- TypeScript compiles clean.
</success_criteria>

<output>
Create `.planning/phases/18-popup-interaction-fixes/18-03-SUMMARY.md` when done.
</output>
