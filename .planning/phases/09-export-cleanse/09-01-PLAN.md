---
phase: 09-export-cleanse
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/dashboard/dataManagement.ts
  - src/dashboard/dataManagement.test.ts
autonomous: true
requirements: [EXPORT-01, EXPORT-02, CLEANSE-01, CLEANSE-02]

must_haves:
  truths:
    - "buildJsonExport joins stored posts to accounts by authorId and emits ISO-string timestamps (D-01, D-03)"
    - "buildCsvExport emits one RFC-4180-escaped row per account with the 10 locked columns in order (D-04, D-05)"
    - "csvEscape doubles internal double-quotes before wrapping a field containing comma/quote/newline"
    - "deriveCleanseCount counts accounts by lastSeenAt and posts by hiddenAt against a UTC-midnight cutoff (D-07, D-08)"
    - "filterCleansed returns the kept accounts Record and kept posts array (timestamps >= cutoff) (D-07, D-08)"
  artifacts:
    - path: "src/dashboard/dataManagement.ts"
      provides: "Pure export + cleanse functions: buildJsonExport, buildCsvExport, csvEscape, deriveCleanseCount, filterCleansed"
      exports: ["buildJsonExport", "buildCsvExport", "csvEscape", "deriveCleanseCount", "filterCleansed"]
      min_lines: 60
    - path: "src/dashboard/dataManagement.test.ts"
      provides: "Vitest coverage of all five functions including the signals-JSON-in-CSV escaping case and UTC cutoff edge"
      contains: "describe"
  key_links:
    - from: "src/dashboard/dataManagement.ts"
      to: "src/shared/types.ts"
      via: "import type FlaggedAccount, StoredPost"
      pattern: "import type.*FlaggedAccount"
---

<objective>
Create `src/dashboard/dataManagement.ts` — a self-contained module of pure, testable functions that implement the Phase 9 export serialisation (JSON + CSV) and cleanse filtering logic, with full Vitest coverage written first (TDD).

Purpose: Isolating the serialisation and filtering logic into pure functions (no DOM, no chrome.* calls) lets us prove the high-risk CSV RFC-4180 escaping (D-05) and the UTC cutoff date logic (D-07/D-08) with deterministic unit tests before any UI wiring exists. Plan 02 consumes these exports.
Output: `src/dashboard/dataManagement.ts` + `src/dashboard/dataManagement.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-export-cleanse/09-CONTEXT.md
@.planning/phases/09-export-cleanse/09-RESEARCH.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Export serialisation functions (JSON + CSV) — tests first</name>
  <files>src/dashboard/dataManagement.ts, src/dashboard/dataManagement.test.ts</files>
  <read_first>
    - src/shared/types.ts (FlaggedAccount, StoredPost field names and types — authorId, authorName, authorProfileUrl, peakScore, compositeScore, postCount, status, signals, firstSeenAt, lastSeenAt; StoredPost: urn, authorId, score, text, hiddenAt)
    - src/content/exclusions.test.ts (existing Vitest conventions: explicit `import { describe, it, expect } from 'vitest'`, jsdom env, no globals)
    - .planning/phases/09-export-cleanse/09-RESEARCH.md (Pattern 3 buildJsonExport, Pattern 4 buildCsvExport + csvEscape, Pitfall 2 signals-in-CSV double-encoding)
    - .planning/phases/09-export-cleanse/09-CONTEXT.md (D-01, D-02, D-03, D-04, D-05)
  </read_first>
  <behavior>
    - csvEscape(72) returns "72" (no quoting for plain numbers)
    - csvEscape('a,b') returns the field wrapped in double-quotes: "a,b"
    - csvEscape('say "hi"') doubles the internal quotes then wraps: "say ""hi"""
    - csvEscape a value containing \n or \r is wrapped in double-quotes
    - buildJsonExport(accounts, posts): returns a JSON string that JSON.parse-es to { exportedAt: <ISO string>, flaggedAccounts: [...] } (D-01, D-02)
    - buildJsonExport embeds posts under each account's `posts` field, joined by authorId; an account with no matching posts gets `posts: []` (D-01)
    - buildJsonExport converts firstSeenAt, lastSeenAt, and each post's hiddenAt from Unix ms to ISO 8601 strings via new Date(ts).toISOString() (D-03)
    - buildJsonExport with empty accounts array returns valid JSON with flaggedAccounts: [] (Pitfall 5)
    - buildJsonExport output does NOT contain a top-level dailyStats or dismissedAccounts key (D-02)
    - buildCsvExport(accounts): first line is exactly the header `authorId,authorName,authorProfileUrl,peakScore,compositeScore,postCount,status,firstSeenAt,lastSeenAt,signals` (D-04)
    - buildCsvExport emits one row per account; rows are joined with CRLF (\r\n) per RFC 4180 (D-05)
    - buildCsvExport compositeScore column is Math.round()-ed to an integer (Research anti-pattern: EMA float)
    - buildCsvExport firstSeenAt/lastSeenAt columns are ISO 8601 strings (D-03)
    - buildCsvExport signals column is JSON.stringify(account.signals) passed through csvEscape — e.g. account.signals {listicle:25,buzzwords:15} produces a field "{""listicle"":25,""buzzwords"":15}" with doubled internal quotes (D-04, D-05, Pitfall 2)
    - buildCsvExport with empty accounts array returns just the header line (Pitfall 5)
    - buildCsvExport never includes post text (D-05)
  </behavior>
  <action>
    Write the test file first (RED), then implement (GREEN). Create `src/dashboard/dataManagement.ts` exporting three functions: `csvEscape(value: string | number): string`, `buildJsonExport(accounts: FlaggedAccount[], posts: StoredPost[]): string`, and `buildCsvExport(accounts: FlaggedAccount[]): string`.

    csvEscape: convert to String; if the string contains any of `,` `"` `\n` `\r`, replace every `"` with `""` then wrap the whole string in outer double-quotes; otherwise return the string unchanged. Quote-doubling MUST happen before wrapping (Pitfall 2).

    buildJsonExport: build a `postsByAuthor` map keyed by StoredPost.authorId; return JSON.stringify of an object `{ exportedAt: new Date().toISOString(), flaggedAccounts: accounts.map(...) }` where each mapped account spreads all FlaggedAccount fields but overrides firstSeenAt and lastSeenAt with new Date(ts).toISOString() and adds `posts` = the matched posts mapped to { urn, score, text, hiddenAt: new Date(p.hiddenAt).toISOString() }. Use 2-space pretty-print (JSON.stringify(payload, null, 2)). Do NOT include dailyStats or dismissedAccounts (D-02).

    buildCsvExport: headers array in the exact D-04 order. For each account build a values array `[authorId, authorName, authorProfileUrl, peakScore, Math.round(compositeScore), postCount, status, new Date(firstSeenAt).toISOString(), new Date(lastSeenAt).toISOString(), JSON.stringify(signals)]`, map each through csvEscape, join with `,`. Return `[headers.join(','), ...rows].join('\r\n')`.

    Import FlaggedAccount and StoredPost as type-only imports from `../shared/types`. No DOM access, no chrome.* calls in this file. Do not place fenced code in the source — follow the Research Pattern 3/4 signatures.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/dataManagement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/dataManagement.test.ts` exits 0 with all export-function tests passing
    - Test file asserts the exact CSV header string `authorId,authorName,authorProfileUrl,peakScore,compositeScore,postCount,status,firstSeenAt,lastSeenAt,signals`
    - Test file includes an assertion that a signals object containing double-quotes (via JSON.stringify) is emitted with doubled internal quotes and outer wrapping
    - Test file includes an empty-accounts case for both buildJsonExport (valid JSON, flaggedAccounts: []) and buildCsvExport (header line only)
    - `npx tsc --noEmit` reports no errors for dataManagement.ts
    - `src/dashboard/dataManagement.ts` contains type-only import matching pattern `import type.*FlaggedAccount`
  </acceptance_criteria>
  <done>All three export functions exist with passing tests covering the CSV escaping edge cases (signals JSON, commas, internal quotes, CRLF) and empty-state handling; timestamps emit as ISO strings; no post text in CSV.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Cleanse filtering functions (count + filter) — tests first</name>
  <files>src/dashboard/dataManagement.ts, src/dashboard/dataManagement.test.ts</files>
  <read_first>
    - src/dashboard/dataManagement.ts (the file Task 1 created — add to it, do not duplicate imports)
    - src/shared/types.ts (FlaggedAccount.lastSeenAt, StoredPost.hiddenAt are Unix ms numbers; StorageSchema.flaggedAccounts is Record<string, FlaggedAccount>)
    - .planning/phases/09-export-cleanse/09-RESEARCH.md (Pattern 5 deriveCleanseCount + executeClean; Pitfall 1 UTC midnight; Pitfall 3 dismissedAccounts is string[]; Pitfall 6 empty-string date → NaN)
    - .planning/phases/09-export-cleanse/09-CONTEXT.md (D-07, D-08, D-09, D-10)
  </read_first>
  <behavior>
    - deriveCleanseCount(accounts, posts, '2026-05-01') returns { accountCount, postCount } where accountCount = accounts with lastSeenAt < cutoff and postCount = posts with hiddenAt < cutoff (D-07, D-08)
    - cutoff is computed as new Date(beforeDateStr).getTime() — date-only string parses as UTC midnight (Pitfall 1); add a comment stating UTC parsing is intentional
    - An account with lastSeenAt exactly equal to the cutoff is NOT counted (strictly-less-than boundary; kept on >= )
    - A post with hiddenAt exactly equal to the cutoff is NOT counted (strictly-less-than)
    - deriveCleanseCount with empty-string date: new Date('').getTime() is NaN, NaN comparisons are false, so both counts are 0 (Pitfall 6)
    - filterCleansed(accounts, posts, '2026-05-01') returns { keptAccounts: Record<string, FlaggedAccount>, keptPosts: StoredPost[] } where kept items have timestamp >= cutoff (inverse of the removed set) (D-07, D-08)
    - filterCleansed keptAccounts is keyed by authorId (rebuilds the Record shape that StorageSchema.flaggedAccounts uses)
    - filterCleansed does not mutate the input accounts or posts arrays
    - filterCleansed does NOT touch or return dailyStats (D-10); dismissedAccounts wipe is the caller's responsibility (D-09) — note this in a comment
  </behavior>
  <action>
    Append to `src/dashboard/dataManagement.ts` (created in Task 1). Add two functions and their tests (tests first, RED→GREEN).

    `deriveCleanseCount(accounts: FlaggedAccount[], posts: StoredPost[], beforeDateStr: string): { accountCount: number; postCount: number }`: compute `const cutoffMs = new Date(beforeDateStr).getTime();` with a comment that date-only strings parse as UTC midnight and stored timestamps are UTC ms, so the comparison is correct; do NOT append 'T00:00:00'. Return accountCount = accounts.filter(a => a.lastSeenAt < cutoffMs).length and postCount = posts.filter(p => p.hiddenAt < cutoffMs).length.

    `filterCleansed(accounts: FlaggedAccount[], posts: StoredPost[], beforeDateStr: string): { keptAccounts: Record<string, FlaggedAccount>; keptPosts: StoredPost[] }`: compute the same cutoffMs; build keptAccounts by iterating accounts.filter(a => a.lastSeenAt >= cutoffMs) into a fresh Record keyed by a.authorId; keptPosts = posts.filter(p => p.hiddenAt >= cutoffMs). Use a non-mutating filter (returns new arrays). Add a comment that dailyStats are untouched (D-10) and dismissedAccounts wiping is handled by the caller (D-09), since dismissedAccounts is string[] with no timestamp to filter on (Pitfall 3).

    Boundary rule: removed = strictly-less-than cutoff; kept = greater-than-or-equal. Tests must assert the exact-equal boundary stays (kept).
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/dataManagement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/dashboard/dataManagement.test.ts` exits 0 with cleanse tests passing alongside Task 1 tests
    - Test asserts an account/post with timestamp exactly equal to cutoff is KEPT (not counted, not removed)
    - Test asserts deriveCleanseCount with '' (empty date) returns { accountCount: 0, postCount: 0 }
    - Test asserts filterCleansed keptAccounts is a Record keyed by authorId
    - `src/dashboard/dataManagement.ts` source contains a comment referencing UTC midnight parsing (matches pattern `UTC`) and uses `new Date(beforeDateStr).getTime()` without a 'T00:00:00' suffix
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>deriveCleanseCount and filterCleansed exist with passing tests proving the UTC cutoff, strictly-less-than removal boundary, empty-date NaN safety, non-mutation, and Record-by-authorId output shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→date input | `cleanseDate` string crosses into cleanse logic (validated by `<input type="date">` to YYYY-MM-DD; NaN-safe in pure functions) |
| storage→export file | Stored post text leaves chrome.storage.local into a downloaded JSON file (intentional user opt-in, v1.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | deriveCleanseCount / filterCleansed with empty or invalid date | mitigate | new Date('').getTime() is NaN; NaN comparisons are false → zero records removed (safe default). Tested explicitly. |
| T-09-02 | Information Disclosure | buildJsonExport includes stored post text | accept | Intentional v1.1 opt-in; export is local-only to the user's own filesystem. CSV (buildCsvExport) excludes post text per D-05. |
| T-09-03 | Tampering | CSV signals field malformed by missing quote-doubling | mitigate | csvEscape doubles internal quotes before outer wrapping (Pitfall 2); covered by a dedicated test case. |
</threat_model>

<verification>
- `npx vitest run src/dashboard/dataManagement.test.ts` passes (all export + cleanse tests).
- `npx tsc --noEmit` reports no type errors.
- Source contains five exports: buildJsonExport, buildCsvExport, csvEscape, deriveCleanseCount, filterCleansed.
</verification>

<success_criteria>
- All five pure functions implemented and exported from src/dashboard/dataManagement.ts.
- CSV escaping proven against the signals-JSON double-quote case.
- Cleanse cutoff proven UTC-correct with strictly-less-than removal boundary and empty-date safety.
- No DOM or chrome.* dependency in this module (fully unit-testable).
</success_criteria>

<output>
Create `.planning/phases/09-export-cleanse/09-01-SUMMARY.md` when done.
</output>
