---
phase: 15-url-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/manifest.json
  - PRIVACY.md
  - store/LISTING.md
  - store/SUBMISSION_GUIDE.md
  - .planning/phases/12-manifest-icons/12-CONTEXT.md
  - .planning/phases/12-manifest-icons/12-02-PLAN.md
  - .planning/phases/13-store-assets/13-CONTEXT.md
  - .planning/phases/13-store-assets/13-01-PLAN.md
  - .planning/phases/13-store-assets/13-02-PLAN.md
  - .planning/phases/14-package-submit/14-CONTEXT.md
  - .planning/phases/14-package-submit/14-02-PLAN.md
autonomous: true
requirements: [RENAME-01, RENAME-02, RENAME-03, RENAME-04, RENAME-05]

must_haves:
  truths:
    - "src/manifest.json homepage_url is https://github.com/ed-is-ai/linkedinaivoiceblock"
    - "PRIVACY.md GitHub Issues link points to linkedinaivoiceblock"
    - "store/LISTING.md privacy policy URL points to linkedinaivoiceblock"
    - "store/SUBMISSION_GUIDE.md homepage URL and privacy policy URL point to linkedinaivoiceblock"
    - "git remote origin URL updated to https://github.com/ed-is-ai/linkedinaivoiceblock.git"
    - "No remaining occurrences of 'linkedinblock' in src/manifest.json, PRIVACY.md, store/LISTING.md, store/SUBMISSION_GUIDE.md"
    - "npm run package exits 0 and produces an updated dist/linkedin-blocker-v1.2.0.zip"
  artifacts:
    - path: "src/manifest.json"
      provides: "Updated homepage_url"
      contains: "linkedinaivoiceblock"
    - path: "PRIVACY.md"
      provides: "Updated GitHub Issues link"
      contains: "linkedinaivoiceblock"
---

<objective>
Replace every `linkedinblock` URL reference with `linkedinaivoiceblock` across live source files and archived planning docs, update the git remote, and rebuild the ZIP so the submitted extension package has the correct homepage URL.

**Prerequisite (user action):** The GitHub repo must already be renamed to `linkedinaivoiceblock` before pushing — otherwise the new URLs won't resolve. The user confirms the rename was done on GitHub settings.

Old base URLs:
- `https://github.com/ed-is-ai/linkedinblock`
- `https://raw.githubusercontent.com/ed-is-ai/linkedinblock`

New base URLs:
- `https://github.com/ed-is-ai/linkedinaivoiceblock`
- `https://raw.githubusercontent.com/ed-is-ai/linkedinaivoiceblock`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update live source files</name>
  <files>src/manifest.json, PRIVACY.md, store/LISTING.md, store/SUBMISSION_GUIDE.md</files>
  <read_first>
    - src/manifest.json
    - PRIVACY.md
    - store/LISTING.md
    - store/SUBMISSION_GUIDE.md
  </read_first>
  <action>
    In each file, replace every occurrence of `ed-is-ai/linkedinblock` with `ed-is-ai/linkedinaivoiceblock`.

    Specific changes:

    **src/manifest.json** — change:
    ```
    "homepage_url": "https://github.com/ed-is-ai/linkedinblock"
    ```
    to:
    ```
    "homepage_url": "https://github.com/ed-is-ai/linkedinaivoiceblock"
    ```

    **PRIVACY.md** — change the GitHub Issues link at the end of the file:
    ```
    https://github.com/ed-is-ai/linkedinblock/issues
    ```
    to:
    ```
    https://github.com/ed-is-ai/linkedinaivoiceblock/issues
    ```

    **store/LISTING.md** — two occurrences:
    1. Privacy policy URL in the detailed description body
    2. Privacy policy URL at the bottom of the file

    Both change `linkedinblock` → `linkedinaivoiceblock`.

    **store/SUBMISSION_GUIDE.md** — two occurrences:
    1. Homepage URL in the store listing table
    2. Privacy policy URL in the privacy practices step

    Both change `linkedinblock` → `linkedinaivoiceblock`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/manifest.json','PRIVACY.md','store/LISTING.md','store/SUBMISSION_GUIDE.md']; let found=false; for(const f of files){const s=fs.readFileSync(f,'utf8'); if(s.includes('ed-is-ai/linkedinblock')){console.error('OLD URL still in '+f);found=true;}} if(found)process.exit(1); console.log('All live files updated');"</automated>
  </verify>
  <acceptance_criteria>
    - No occurrence of `ed-is-ai/linkedinblock` remains in any of the four live files
    - All four files contain `ed-is-ai/linkedinaivoiceblock` in place of the old URL
  </acceptance_criteria>
  <done>Live source files updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update archived planning docs</name>
  <files>
    .planning/phases/12-manifest-icons/12-CONTEXT.md,
    .planning/phases/12-manifest-icons/12-02-PLAN.md,
    .planning/phases/13-store-assets/13-CONTEXT.md,
    .planning/phases/13-store-assets/13-01-PLAN.md,
    .planning/phases/13-store-assets/13-02-PLAN.md,
    .planning/phases/14-package-submit/14-CONTEXT.md,
    .planning/phases/14-package-submit/14-02-PLAN.md
  </files>
  <read_first>
    - (Read each file only as needed — they all contain `ed-is-ai/linkedinblock` per the grep inventory)
  </read_first>
  <action>
    In each of the seven planning files, replace every occurrence of `ed-is-ai/linkedinblock` with `ed-is-ai/linkedinaivoiceblock`.

    These are historical planning artefacts — no acceptance test needed beyond the text replacement. Use Edit with `replace_all: true` for each file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),path=require('path'); const dirs=['.planning/phases/12-manifest-icons','.planning/phases/13-store-assets','.planning/phases/14-package-submit']; let found=false; for(const d of dirs){const files=fs.readdirSync(d).map(f=>path.join(d,f)); for(const f of files){const s=fs.readFileSync(f,'utf8'); if(s.includes('ed-is-ai/linkedinblock')){console.error('OLD URL still in '+f);found=true;}}} if(found)process.exit(1); console.log('All planning docs updated');"</automated>
  </verify>
  <acceptance_criteria>
    - No `ed-is-ai/linkedinblock` references in the seven planning files
  </acceptance_criteria>
  <done>Archived planning docs updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update git remote and rebuild ZIP</name>
  <files>(git config + dist/linkedin-blocker-v1.2.0.zip)</files>
  <read_first>
    - (No file read needed — git remote update and package script run)
  </read_first>
  <action>
    **1. Update git remote URL:**
    ```bash
    git remote set-url origin https://github.com/ed-is-ai/linkedinaivoiceblock.git
    ```

    **2. Verify the remote was updated:**
    ```bash
    git remote get-url origin
    ```
    Should output: `https://github.com/ed-is-ai/linkedinaivoiceblock.git`

    **3. Rebuild the ZIP** so it contains the updated `manifest.json`:
    ```bash
    npm run package
    ```
  </action>
  <verify>
    <automated>node -e "const {execSync}=require('child_process'); const url=execSync('git remote get-url origin').toString().trim(); if(!url.includes('linkedinaivoiceblock')){console.error('remote not updated:',url);process.exit(1);} const fs=require('fs'); if(!fs.existsSync('dist/linkedin-blocker-v1.2.0.zip')){console.error('ZIP missing');process.exit(1);} console.log('remote ok:',url); console.log('ZIP ok:',Math.round(fs.statSync('dist/linkedin-blocker-v1.2.0.zip').size/1024),'KB');"</automated>
  </verify>
  <acceptance_criteria>
    - `git remote get-url origin` returns the new `linkedinaivoiceblock` URL
    - `dist/linkedin-blocker-v1.2.0.zip` exists and is > 10 KB (rebuilt)
  </acceptance_criteria>
  <done>Git remote updated, ZIP rebuilt with corrected manifest.</done>
</task>

</tasks>

<threat_model>
No security threats — URL string replacements only. No logic changes.
</threat_model>

<verification>
- `node -e` grep check confirms no remaining `ed-is-ai/linkedinblock` in live files
- `git remote get-url origin` returns the new URL
- `npm run package` exits 0
</verification>

<success_criteria>
- All RENAME-01 through RENAME-05 requirements satisfied
- The ZIP submitted to CWS will have the correct homepage_url
- The privacy policy raw GitHub URL in the CWS listing will resolve correctly after the repo is renamed on GitHub
</success_criteria>

<output>
Create `.planning/phases/15-url-rename/15-01-SUMMARY.md` when done.
</output>
