---
phase: 12-manifest-icons
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/manifest.json
  - package.json
autonomous: true
requirements: [CWS-01]

must_haves:
  truths:
    - "manifest.json version is '1.2.0'"
    - "manifest.json has an 'icons' field with keys '16', '48', '128' pointing to icons/icon-16.png etc."
    - "manifest.json action.default_icon has keys '16' and '48'"
    - "manifest.json has homepage_url set to the GitHub repo URL"
    - "package.json version is '1.2.0'"
    - "npx vite build exits 0 and dist/ contains the three icon PNG files"
    - "npx tsc --noEmit exits 0"
  artifacts:
    - path: "src/manifest.json"
      provides: "CWS-compliant manifest"
      contains: "icons"
---

<objective>
Update `manifest.json` (and `package.json`) to pass Chrome Web Store technical validation.

Six changes to `src/manifest.json`:
1. Bump `version` to `"1.2.0"`
2. Add `"icons"` with 16/48/128 entries
3. Add `"default_icon"` to the `"action"` object with 16/48 entries
4. Add `"homepage_url"` pointing to the GitHub repo
5. Improve `"description"` to be more store-ready (current: "Hides AI-generated posts on LinkedIn.")
6. Keep `api.anthropic.com` in `host_permissions` as-is — the LLM feature is real and will be documented in the store listing

One change to `package.json`:
- Bump `version` to `"1.2.0"` to match the manifest

Then run `npx vite build` to confirm the build succeeds and icons are copied to `dist/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-manifest-icons/12-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update manifest.json with all CWS-required fields</name>
  <files>src/manifest.json</files>
  <read_first>
    - src/manifest.json (full file — current state)
    - src/icons/ (confirm icon-16.png, icon-48.png, icon-128.png exist from Plan 01)
  </read_first>
  <action>
    Replace `src/manifest.json` with the following (preserving all existing fields, adding the new ones):

    ```json
    {
      "manifest_version": 3,
      "name": "LinkedIn Blocker",
      "version": "1.2.0",
      "description": "Automatically hides AI-generated posts on LinkedIn using heuristic scoring. Flags suspicious accounts for review, with optional Claude AI-powered detection.",
      "homepage_url": "https://github.com/ed-is-ai/linkedinaivoiceblock",
      "icons": {
        "16": "icons/icon-16.png",
        "48": "icons/icon-48.png",
        "128": "icons/icon-128.png"
      },
      "permissions": ["storage", "activeTab"],
      "host_permissions": ["https://www.linkedin.com/*", "https://api.anthropic.com/*"],
      "background": {
        "service_worker": "background/index.ts",
        "type": "module"
      },
      "content_scripts": [
        {
          "matches": ["https://www.linkedin.com/*"],
          "js": ["content/index.ts"],
          "run_at": "document_idle",
          "all_frames": false
        }
      ],
      "action": {
        "default_popup": "popup/index.html",
        "default_title": "LinkedIn Blocker",
        "default_icon": {
          "16": "icons/icon-16.png",
          "48": "icons/icon-48.png"
        }
      },
      "options_ui": {
        "page": "dashboard/index.html",
        "open_in_tab": true
      }
    }
    ```

    Key changes from current:
    - `version`: `"0.1.0"` → `"1.2.0"`
    - `description`: improved to be store-ready and mention both detection modes
    - Added `homepage_url`
    - Added `icons` object
    - Added `action.default_icon`
  </action>
  <verify>
    <automated>node -e "const m = JSON.parse(require('fs').readFileSync('src/manifest.json','utf8')); const checks = [m.version==='1.2.0', !!m.icons, !!m.icons['16'], !!m.icons['128'], !!m.action.default_icon, !!m.homepage_url]; const fail = checks.filter(c=>!c); if(fail.length){console.error('manifest check failed');process.exit(1);} console.log('manifest.json ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest_version` is still 3
    - `version` is `"1.2.0"`
    - `icons` has keys `"16"`, `"48"`, `"128"` pointing to `icons/icon-*.png`
    - `action.default_icon` has keys `"16"` and `"48"`
    - `homepage_url` is present
    - All existing fields (`permissions`, `host_permissions`, `background`, `content_scripts`, `action`, `options_ui`) are preserved
  </acceptance_criteria>
  <done>manifest.json updated with all CWS-required fields.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bump package.json version and verify build</name>
  <files>package.json</files>
  <read_first>
    - package.json (full file)
  </read_first>
  <action>
    In `package.json`, change:
    ```json
    "version": "0.1.0"
    ```
    to:
    ```json
    "version": "1.2.0"
    ```

    Then run the build to confirm icons are included in output:
    ```bash
    npx vite build
    ```
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); if(p.version!=='1.2.0'){console.error('version wrong');process.exit(1);} console.log('package.json version ok');" && node -e "const fs=require('fs'); const icons=['dist/icons/icon-16.png','dist/icons/icon-48.png','dist/icons/icon-128.png']; const miss=icons.filter(f=>!fs.existsSync(f)); if(miss.length){console.error('missing in dist:',miss);process.exit(1);} console.log('dist icons ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` version is `"1.2.0"`
    - `npx vite build` exits 0
    - `dist/icons/icon-16.png`, `dist/icons/icon-48.png`, `dist/icons/icon-128.png` all exist after build
  </acceptance_criteria>
  <done>Version bumped, build passes, icons present in dist/.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-12-02 | CWS policy | api.anthropic.com host_permission | accept | Permission is used for an opt-in LLM feature — user must manually set an API key. Will be documented in store listing (Phase 13). CWS reviewers accept this for clearly described optional features. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vite build` exits 0
- `dist/icons/` contains all three PNG files
- `dist/manifest.json` (the built output) has the correct version, icons, and homepage_url
</verification>

<success_criteria>
- CWS-01 and CWS-02 satisfied: manifest has all required fields; icons at three sizes are in dist/
- Extension can be loaded as an unpacked extension in Chrome (prerequisite for Phase 14 ZIP test)
</success_criteria>

<output>
Create `.planning/phases/12-manifest-icons/12-02-SUMMARY.md` when done.
</output>
